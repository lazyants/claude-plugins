# Lazy Ants — Claude Code plugins

Public plugins for [Claude Code](https://claude.com/claude-code), maintained under the `Lazy Ants` brand.

## Plugins

| Plugin | Version | What it does |
|---|---|---|
| [`ai-cli-optout`](#ai-cli-optout--v112) | 1.1.2 | Opt out of telemetry across every locally installed AI CLI / AI-enabled IDE, plus Vercel CLI and macOS / Windows OS-level privacy surfaces. |
| [`db-guardrails`](#db-guardrails--v100) | 1.0.0 | Stop AI coding agents from accidentally emptying your database — an always-on hook that blocks destructive DB commands across 15+ frameworks, plus a stack-aware installer for deeper safety layers. |
| [`obsidian-project-vault`](#obsidian-project-vault--v100) | 1.0.0 | Set up, migrate, audit, and operate an Obsidian vault as an LLM Wiki — a persistent, compounding knowledge base maintained by Claude Code. |
| [`cc-usage-coach`](#cc-usage-coach--v100) | 1.0.0 | Personalized, behavior-aware analysis of where your Claude Code (Max/Pro) usage-limit tokens go, with ranked, low-effort ways to use fewer — computed entirely from your local session logs. Python measures; Claude concludes. |
| [`enduser-handbook`](#enduser-handbook--v1181) | 1.18.1 | Author, capture, and publish a Diátaxis-structured end-user handbook for any project — methodology shipped as a reusable skill, project-specific bindings supplied via `.claude/handbook/profile.yml`. |
| [`literary-translator`](#literary-translator--v1620) | 1.62.0 | High-fidelity literary book translation over a Gutenberg-style EPUB source (expert-mode `custom` extractor also supported) — a codex-translate → deterministic false-green gate → codex-review → Claude-fix loop run to convergence, with a frozen name/realia canon, a configurable verse policy, and ledger-based resumability, plus optional book assembly into an Obsidian glossary-wiki behind a deterministic render/diff gate. |
| [`multi-profile-plugins`](#multi-profile-plugins--v100) | 1.0.0 | Understand and diagnose Claude Code plugin behavior across multiple `CLAUDE_CONFIG_DIR` profiles — why profiles that share a plugins store hit recurring "corrupted installLocation" errors and cross-profile plugin deletion, with a read-only health-check script and the reverse-engineered CLI validation/GC mechanism behind it. |

> **Changelogs.** Every plugin's release notes are in the root [`CHANGELOG.md`](CHANGELOG.md) — except `literary-translator`, which keeps its own at [`plugins/literary-translator/CHANGELOG.md`](plugins/literary-translator/CHANGELOG.md). The root file is frozen for that plugin at its `1.1.0` entry, so its later releases and its Known limitations are only in the per-plugin file.

## Install / update / uninstall

```
claude plugin marketplace add lazyants/claude-plugins
claude plugin install <plugin-name>@lazyants
```

Restart Claude Code once after install for new skill triggers to register. The `@lazyants` marketplace suffix is required on every plugin command — bare `claude plugin update <name>` will not find the plugin.

```
claude plugin update <plugin-name>@lazyants
claude plugin uninstall <plugin-name>@lazyants
```

## `ai-cli-optout` — v1.1.2

Opts out of telemetry, error reporting, analytics, feedback surveys, and related data collection across every locally installed AI CLI and AI-enabled IDE, plus Vercel CLI (adjacent developer tooling) and macOS / Windows OS-level privacy surfaces. One skill, thirteen vendors, data-driven. 369 test assertions guard vendor-schema invariants and script behavior.

Trigger phrases: "disable telemetry", "opt out of telemetry", "privacy mode", etc. — full list in `plugins/ai-cli-optout/skills/ai-cli-optout/SKILL.md`.

### Vendors covered (baseline 2026-04-24)

| Vendor | Platform | Kind |
|---|---|---|
| Anthropic Claude Code | any | settings.json + env *(2 edits confirmation-gated — see warnings)* |
| OpenAI Codex CLI | any | `~/.codex/config.toml` |
| Google Gemini CLI | any | settings.json + env |
| GitHub Copilot CLI + `gh` | any | `gh config set` + env |
| Cursor | darwin | manual only — Cmd+Shift+J → Privacy Mode |
| Cursor CLI (`cursor-agent`) | any | manual only — account-level Privacy Mode |
| Google Antigravity | darwin | settings.json (AI-training opt-out is email-only) |
| VS Code | darwin | settings.json (Copilot extension does not inherit) |
| PhpStorm | darwin | manual only — Settings → Tools → Usage Statistics |
| Vercel CLI | any | `vercel telemetry disable` (persistent) + `VERCEL_TELEMETRY_DISABLED=1` (per-run) |
| Vercel Claude Code plugin | any | env — `VERCEL_PLUGIN_TELEMETRY=off` (interim, not yet first-class) |
| macOS system privacy | darwin | `defaults write` (AdLib, CrashReporter) |
| Windows system privacy | win32 | `reg add` (Recall, Copilot, Telemetry, AdvertisingInfo) |

### Warnings before you run it

- **Anthropic Claude Code users:** setting `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1` or `DISABLE_TELEMETRY=1` does more than stop telemetry — both flags also disable the `/remote-control` command and, on Max / Team / Enterprise plans, silently switch Opus 4.6 off the 1M-context default model. See [anthropics/claude-code#34178](https://github.com/anthropics/claude-code/issues/34178) and #142. The skill gates both edits behind `requires_confirmation: true` so you see the trade-off and can decline. A safe narrow opt-out that leaves these features intact (`DISABLE_ERROR_REPORTING=1`, `DISABLE_FEEDBACK_COMMAND=1`, `CLAUDE_CODE_DISABLE_FEEDBACK_SURVEY=1`, `skipWebFetchPreflight: true`) is documented in #142.
- **Cursor:** quit the app before the skill edits any JSON — Cursor's Electron process rewrites settings on graceful quit and will overwrite your changes.
- **Antigravity:** the AI-training opt-out is **email-only** to `antigravity-support@google.com`; no CLI / setting exists. The skill surfaces this in its report but cannot automate it.
- **Vercel CLI:** the subcommand `vercel telemetry disable` is persistent; `VERCEL_TELEMETRY_DISABLED=1` is per-run only and does **not** change the persisted status reported by `vercel telemetry status`. The skill applies both so you're covered either way.
- **Vercel Claude Code plugin (separate from the Vercel CLI):** if installed, it sends every bash command string to `telemetry.vercel.com` by default. Interim opt-out: `VERCEL_PLUGIN_TELEMETRY=off`. Not yet a first-class vendor in this skill — tracked in #144.

### What this does **not** cover

- Prompts and outputs still go to each vendor's API. To avoid that path you must switch providers (e.g. route Claude Code to AWS Bedrock / Google Vertex / Azure Foundry).
- Existing local state (Codex sqlite, conversation logs, OAuth tokens) is **reported**, never deleted.
- Cursor AI telemetry when Privacy Mode is off — the only vendor-blessed control is the UI toggle.

See [`DISCLAIMER.md`](./DISCLAIMER.md) for the full no-warranty statement.

## `db-guardrails` — v1.0.0

Stop AI coding agents from accidentally emptying your database. It exists because it happened — an agent ran `artisan migrate` with a test flag that did *not* isolate to the test database and wiped the development database. Twice. `db-guardrails` is the hardened, generalised result.

Trigger phrases: "harden the database", "protect the database", "db guardrails", "stop dropping the database", "database privilege separation", etc. — full list in `plugins/db-guardrails/skills/db-guardrails/SKILL.md`.

### What it does

- **Layer 4 — the hook (auto-on).** A `PreToolUse:Bash` hook blocks destructive database commands the moment the plugin is installed, in **any** project. Recognised across 15+ stacks: raw SQL (`DROP`, `TRUNCATE`, `DELETE` without `WHERE`), Laravel, Rails, Django, Prisma, TypeORM, Sequelize, Knex, Drizzle, Doctrine/Symfony, EF Core, Alembic, Flyway, Liquibase, MongoDB, Redis, plus `docker compose down -v` and `rm -rf` of DB data directories. Blocked attempts are logged to `~/.claude/logs/destructive-db-blocked.log`.
- **Layers 1–3 — the `/db-guardrails` skill.** Run it once per project. It detects the database engine and framework, then scaffolds database-level privilege separation (the app role loses `DROP` — works for MySQL/MariaDB and PostgreSQL), a framework boot guard, and test-environment isolation.

### The bypass

The hook is bypassed only by starting Claude Code with `ALLOW_DESTRUCTIVE_DB_HOOK=true` in the shell — a deliberate, out-of-band human action. There is no inline comment or flag that re-enables a single command, by design: an LLM could append a sentinel to any command to bypass its own guard.

### Dependency

The hook parses its input with `jq` (preferred) or `python3` — at least one must be on `PATH`. If neither is found the hook warns and allows rather than breaking every Bash command, so install `jq`.

### What the hook is not

It is a fast heuristic — instant, legible feedback that catches the *accidental* destructive command. It is not a hard guarantee; a command can be phrased past a regex, and it cannot stop a non-Claude actor. That is why layer 1 (database privilege separation) exists — run the skill.

## `obsidian-project-vault` — v1.0.0

Set up, migrate, audit, and operate an Obsidian vault as an **LLM Wiki** — a persistent, compounding knowledge base maintained by Claude Code instead of a one-shot RAG retrieval surface. Three-layer architecture (raw sources / wiki / schema), four setup modes (create, migrate, audit, ingest), and a query-and-file-back loop so every answer the LLM derives is folded back into the vault.

Trigger phrases: "set up obsidian", "migrate vault", "audit vault", "wiki-lint", "ingest sources", etc. — full list in `plugins/obsidian-project-vault/skills/obsidian-project-vault/SKILL.md`.

### What it covers

- **Setup modes** — create a fresh `vault/` subfolder, migrate an existing standalone vault into a project repo (with diff-before-delete safety), or audit a vault's structural health.
- **Wiki pattern** — three layers (raw sources, wiki, schema), Report template with frontmatter, INDEX.md navigation, CLAUDE.md workflow integration.
- **Ongoing operations** — ingest new sources, query the vault and file findings back, lint vault health, prune stale entries.
- **Git + `.obsidian/`** — `.gitignore` patterns, vault MCP config, sane defaults for human-side workflow (Web Clipper, Dataview, graph view).

## `cc-usage-coach` — v1.0.0

Personalized, behavior-aware analysis of where your Claude Code (Max / Pro) usage-limit tokens go, with ranked, low-effort ways to use fewer — computed entirely from your **local** session logs. Python measures; Claude concludes.

Trigger phrases: "where do my tokens go", "why am I hitting the usage limit", "usage coach", "analyze my Claude Code usage", "how to use fewer tokens", etc. — full list in `plugins/cc-usage-coach/skills/cc-usage-coach/SKILL.md`.

### What it does

- **Builds a path-free signal pack.** A skill reads your local session logs and runs `scripts/extract.py` (logs → local `dataset/`) then `scripts/signals.py` (dataset → `signal_pack.json` + a local-only `source_index.json`). The signal pack is an aggregate of your token shapes, cache patterns, tool mix, and session lengths — no paths, no prompt text.
- **Writes a personalized report.** The Claude runtime reads `signal_pack.json` and produces a plain-language breakdown of where your limit tokens go plus a ranked list of low-effort levers tailored to how you actually work — not generic advice.
- **Per-session arc.** `scripts/arc.py <source_ref>` inspects a single session's prompt arc (referenced by an opaque `source_ref`) so you can see how one conversation consumed budget over time. Local-only.

### Privacy

The **scripts** are local-first: they read local logs only and make no network calls of their own. `signal_pack.json` is path-free and safe to share; `source_index.json`, `project_index.json`, the `dataset/`, and the `arc.py` digest are local-only — they hold real paths, project names, and prompt text, are written `0600` where applicable, and must never be uploaded. Sessions are referred to only by an opaque `source_ref`. The **report**, though, is written by the Claude Code model: the skill sends it the signal pack and (for sessions inspected via `arc.py` in step 4) raw prompt excerpts as prompt context — so on Max/Pro that data goes to Anthropic's API like any Claude Code conversation. Those excerpts are never added to the shareable pack, but the report step is not "nothing leaves your machine."

### Environment variables

- `CLAUDE_CONFIG_DIR` — honored; points the scan at a non-default config directory.
- `CC_COACH_CONFIG_DIRS` — comma-separated extra config dirs to scan (default scans only the standard `.claude`).
- `CC_COACH_OUT` — output location. Precedence: `$CC_COACH_OUT` if set, else next to the scripts if writable, else `${XDG_CACHE_HOME:-~/.cache}/cc-usage-coach/`.

## `enduser-handbook` — v1.18.1

Author, capture, and publish a Diátaxis-structured end-user handbook (tutorials, how-tos, reference, explanation) for any project. The methodology — pre-read mandate, anti-fabrication rules, capture safety, page identity, manifest discipline, glossary and tone consistency, completeness gate, "running UI is the primary source" — ships as a reusable skill. Project-specific bindings (language, register, stack globs, capture engine, publish target, glossary) live in `.claude/handbook/profile.yml` so the same skill produces a German shopkeeper-register handbook for one project and an English developer-register handbook for the next without forking the workflow.

Sibling to [`obsidian-project-vault`](#obsidian-project-vault--v100): where `obsidian-project-vault` builds the *internal* LLM Wiki that the team and Claude Code use, `enduser-handbook` builds the *external* end-user manual that ships to customers. They compose — `enduser-handbook`'s default publish-target adapter is `obsidian-vault`, so the handbook can be written straight into a vault scaffolded by `obsidian-project-vault` (separate folder, separate INDEX wiring, separate frontmatter shape). One vault, two audiences.

Trigger phrases: "write the end-user handbook", "update the user manual", "add a handbook chapter for <feature>", "re-capture handbook screenshots", etc. — full list in `plugins/enduser-handbook/skills/enduser-handbook/SKILL.md`.

### What it covers

- **Profile-driven** — language, register, stack/route globs, capture engine, publish target, glossary discipline all declared in `.claude/handbook/profile.yml`. The skill halts loudly if the profile is missing or unknown rather than guessing.
- **Running UI is the source** — code only tells the skill *which* features and routes exist; every described feature must be captured live, never fabricated from the codebase.
- **Diátaxis structure** — tutorials, how-tos, reference, and explanation each have their own discipline; chapters are gated on completeness before publish.
- **Publish-target adapters** — currently `obsidian-vault` and `static-md` (universal plain-Markdown) — paths, INDEX wiring, link syntax, frontmatter shape governed by the adapter, not improvised.
- **Month-over-month consistency** — mandatory pre-read of style guide + every reference file every session, so tone and terminology stay stable as the handbook grows.
- **Reference capture tooling (v1.0.5)** — ships non-normative Playwright reference implementations for the parts that carry the most risk: a live-DOM surface enumerator (every control's verbatim text/title/aria-label/href/role, icon-only controls included) and a context-level capture guard (fail-closed request classifier, service-worker block, WebSocket/beacon/SSE handling, safe Escape-first dialog dismiss, reproducible PII mask + leak-assert). The methodology stays engine-agnostic — reimplement the driver glue for another engine; the engine-neutral `assets/lib/*.mjs` helpers are reused as-is.
- **Revalidation / audit mode (v1.0.5)** — a first-class path for re-validating an already-merged chapter: re-derive the surface from the running UI, diff against the existing chapter and manifest, and classify each delta (no-op / accepted-diff / material) — material deltas still emit a delta manifest and halt for review.
- **Surface / guard / capture hardening (v1.0.6)** — the surface enumerator now also catches framework glyph/icon controls (`.btn`, `[data-bs-toggle]`, `[data-toggle]`) and records each control's `class` to drive destructive-action hints; the capture guard adds a `'benign'` verdict so blocked dev-telemetry (e.g. laravel-boost, Sentry) no longer false-trips the safety assertion; `captureRegion` gains a `maxHeight` cap for runaway-height modals; and the completeness gate ships a concrete disclose trigger-list + prose templates.
- **Capture-safety correctness (v1.1.1)** — a broken `<img>`'s `alt` text *is* painted into the screenshot (browser replacement-rendering), so it is reclassified from a "non-rendered attribute" to a painted-but-unscannable surface the eyeball backstop must catch — only `title`/`aria-label` are genuinely non-rendered; and the mask/leak-scan scope rule is made explicitly bidirectional — scope must equal the captured frame, so a full-viewport shot scopes the scan to the document root (an element- or region-scoped shot to its own node) and framed app chrome (e.g. a header user name) is never left unscanned.
- **Capture-determinism guardrails (v1.1.1)** — `page-identity.md` now warns against four author-time traps that ship a wrong or broken shot while the run still looks green: asserting visibility on a layout wrapper that collapses to zero height (assert a content-bearing child, not a bare container); capturing a mid-animation frame (settle transitions / disable animations first); a full-element shot of lazy-loaded or virtualized content shipping blank below-the-fold rows (scroll to load first); and a deliberately staged data-state silently reverting (pair every precondition with a fail-closed assertion that it held).
- **Capture-guard and audit hardening (v1.1.2)** — the dangerous-verb detector now percent-decodes to a fixed point, so a doubly-encoded destructive verb in a GET can no longer slip past the deny step; `dismissModal` waits for the dialog to actually hide after Escape; the coverage matrix labels a native `<input type=submit value=Delete>` instead of printing `(unlabelled control)`; and the module's real Playwright floor (>= 1.51) is documented.
- **Authoring ergonomics + coverage (v1.2.0)** — a dependency-free profile validator and `profile_version` reader (`assets/lib/profile-version.mjs` + a normative JSON-Schema) checks the profile at Step 0 without a YAML parser; a new `/scaffold-profile` command generates `.claude/handbook/profile.yml` interactively from an auto-detected, user-confirmed stack; capture now covers **real** empty / error / denied state variants (never synthesized) behind a fail-closed `state` marker on `assertIdentity`; an optional per-role surface re-audit diffs the interactive surface between roles on a PII-free structural key (`tag / role / name / data-testid`); and a `capture-engines.md` reference documents the Playwright / Cypress / Puppeteer / manual recipes and their guard obligations.
- **Cross-line structural profile validation (v1.3.0)** — the dependency-free `profile_version` scan now catches two additional structural error classes, both provably false-reject-free (differential-tested against Ruby's Psych, never halting a document a real YAML parser would load): mechanism A (an unterminated flow collection or quoted scalar anywhere in the document) and mechanism C (an alias to an undefined anchor, in a document with no `&anchor` defined at all). Mechanism B (invalid dedent, including through the block-scalar `capture.command: |` shape) is deliberately deferred — it reintroduces the mini-YAML-parser mis-parse risk the scan otherwise avoids — and is tracked as a follow-up.
- **Profile-scan false-reject + wrong-version fixes (v1.4.0)** — the dependency-free `profile_version` scan no longer halts on a tab inside block/quoted/flow scalar content, a leading `---` document-start marker, a trailing `...` marker, or a plain `? snake_case` explicit key (all differential-tested against Ruby's Psych; genuine multi-document streams — including a trailing bare `---` — still halt), and no longer mis-reports a version for a leading-zero (octal-ambiguous) or above-2⁵³ integer (both now halt). Step 0 additionally warns when a `capture.role_flags` key is not a declared `capture.auth_role_enum` role. Mechanism B (invalid dedent) and non-`? snake_case` explicit keys remain documented residuals. (#125, #126, #127, #155)
- **Bleed-free oversize capture + portable embeds (v1.4.1)** — a new `captureRegionClipped` reference helper takes a single viewport-clipped shot for an overlay taller than the viewport (no scroll-stitch seam that let a `position:fixed` page-behind bleed across), freezes animations for a deterministic settled frame, and publishes atomically so a PNG at the output path is always trustworthy proof; the `obsidian-vault` publish adapter now derives chapter image embeds from `capture.output_dir` (portable across any vault layout — no more broken embeds when `output_dir` is not `<chapters>/assets`); and the `dismissModal` safe-negative-label gate — the last check before a destructive-button click — is lifted into a unit-tested `assets/lib/dismiss-safe-label-policy.mjs`.
- **Optional group axis for two-level grouped nav (v1.5.0)** — manifest entries may declare a `group` (kebab-case directory axis) plus a localized `group_title`; both publish adapters then write chapters to `chapters_dir/<group>/<slug>.md` with grouped asset dirs and two-level nav wiring, backed by manifest-review validation gates (globally-unique slugs, reserved `assets` group/slug, group/flat-slug collision, title rules) and a manual group-migration boundary (halt-is-the-record: retained-entry group/title changes and grouped removals halt with a structured, context-free-reconstructible record; automated migration is deliberately descoped to a follow-up). Strictly additive — a group-free manifest keeps the shipped flat layout byte-identically. Closes #19.
- **Write-time link canon + unconditional duplicate-slug halt (v1.6.0)** — group-free manifests now use the same full-target `relative()` link formula as grouped ones for wikilinks-off glossary/Related links and `static-md` embeds, so those links resolve instead of 404ing (#220); and a duplicate flat slug in a group-free manifest now HALTS instead of silently overwriting the chapter file and its asset dir — `validateGroups` used to return `[]` for group-free manifests, so the duplicate-slug gate never ran there (#221). This is write-time canon only, a deliberate and user-ratified scope boundary: chapters authored before 1.6.0 keep their existing spelling, and nothing is rewritten retroactively just because of an upgrade. The deferred retroactive-repair engine is tracked as #246.
- **De-hardcoded doc-test coverage + single-site glossary link + grouped-precision flat INDEX target (v1.7.0)** — `tests/reference-assets.test.sh` derives its enumeration (both its executable-unit-test block and its doc-scan lists) from their respective source directories via the fail-closed helper instead of hardcoded name lists, so a new file is covered automatically instead of only once someone remembers to list it — this had left `profile-version.differential.test.mjs` never executed (#262). The Obsidian glossary wikilink's two independently-wrong spellings collapse into one vault-root-relative `relative()` form stated at a single site (#247). The flat / group-free INDEX target is now specified at the same grouped-level precision in both adapters (#248). Also: a newly-bound Obsidian vault root with three new halts (zero-marker, ambiguous-marker — an unsupported topology, not a correctable error — and unreadable-ancestor); and `publish.glossary_seed` becomes conditional in the Obsidian adapter — only the unset/unreadable branch changes, from silently required to skipped, correcting the adapter to the base skill's existing "when set and readable" contract.
- **Conflicting-title halt names every title + test-suite mutant hardening (v1.7.1)** — the conflicting-`group_title` manifest halt now enumerates ALL distinct titles in the group instead of only the first two, so an operator no longer re-hits the same halt on a third title it never named (#250). The rest hardens the `chapter-paths` unit tests against mutants the shipped fixtures could not catch: `manualMigrationChecklist`'s `output_dir`/`chapters_dir`/`index_file` roots are exercised fully decoupled (#253), the fence (`runLen >= openLen`) and inline-code (`runLen === openLen`) delimiter-length rules are tested with unequal opener/closer runs (#254), and the post-migration scan-failure halt is pinned with a multi-broken-link fixture (#255).
- **Vault-root-relative chapter wikilinks + exported `currentIndexExpectedTarget` (v1.8.0)** — the Obsidian adapter's wikilinks-mode chapter links and INDEX targets are now emitted vault-root-relative (Obsidian tier-3 exact-match) instead of the bare chapter slug, which only disambiguated when that slug was unique across the *whole* vault rather than just the handbook, so a same-named foreign vault note could shadow a nested chapter's link (#294). A legacy bare `[[slug]]` row from a pre-1.8.0 publish is recognized and upgraded in place on next publish instead of being duplicated. `currentIndexExpectedTarget` (`chapter-paths.mjs`) — previously private, reachable only through the Step-0 write flow, and asserted by no direct test — is now exported, declared in `chapter-paths.d.mts`, and covered by direct unit tests (#295).
- **Nested-list index container automation (v1.10.0)** — a grouped chapter on a GitBook `SUMMARY.md`-style nested-list index is now wired automatically instead of always halting for manual container creation: `wireNestedListChapter` finds or creates a plain-label container bullet and inserts the chapter line under it (both adapters, EOL-faithful), deferring inline-markup labels, `*`/`+` bare-path rows, YAML `nav:`, and path tables to the existing manual halt — a deliberately conservative, user-ratified boundary that never deletes anything (#223). Its worst case was described as a single cosmetic duplicate; 1.11.0 measured that to be an understatement. When a chapter's manifest `title` broke its row's target parse and the emitted child marker was `-`, 1.10.0 appended one more row on every publish without limit. With an emitted `*`/`+` child, 1.10.0 instead wrote one poisoned row and the next scan refused the whole file. Nothing was lost, but neither outcome was the claimed one-off cosmetic residual. See "Nested-list automation limits" in either publish-target adapter for the measured cases.
- **Non-heading placement verification + convergent manual halt (v1.11.0)** — for a bounded, unambiguous subset of non-heading (nested-list) indexes, a new `verifyNonHeadingPlacement` checks which container a grouped chapter's already-present index line sits under against the entry's current `group_title`, halting only on a genuine mismatch; every other non-heading shape (native/YAML nav configs, wildcards, ordered lists, `<!--nav-->` markers, ambiguous matches) is unchanged: nothing further verifies placement and no confirmation is requested, so the run continues unverified exactly as it did before 1.11.0. The existing manual `not-a-list` halt also now tells the operator exactly what shape the very next run will recognize, so one matching edit converges instead of re-hitting the same halt, provided the row carries a plain title — the halt asks for one outright rather than stating which outcome any particular markup produces. That convergence promise is scoped to the shape it names, and the gate behind it judges a two-line candidate **in isolation**, never the operator's real index: if that index carries an inert region anywhere — an unclosed fence, an HTML comment, inline code — the accepted edit still will not be recognized, and the halt repeats. Measured. See "Nested-list automation limits" in either adapter (#329, #330).
- **Build provenance (v1.12.0)** — a published chapter can say which build of the software it describes. An optional `capture.build_identity{command, ui_read}` resolves an identity at capture open and close (and again at W6, to classify the delta); a run record and a per-chapter record are written under a `.provenance/` root that gate 5 requires to be physically disjoint from `capture.output_dir`, halting only for profiles that actually configured provenance so existing flat-topology handbooks keep working with one warning. Crash recovery is a total function over `(token, record, temps)` observed after a no-follow/regular/`nlink===1` hazard gate: nine states, each with the one repair permitted to act on it, and a progress chain so a crash *inside* a repair resumes rather than refuses. The opening digest is RFC 8785 (JCS) implemented in-tree, pinned by an independently computed UTF-8 vector. (#362)
- **`wireNestedListChapter` gains its own membership check (v1.11.0)** — closes the unbounded index-row growth described above **for an unchanged manifest**: the writer now runs a literal, content-verbatim check for whether the target container already carries the exact chapter link it is about to write, and refuses to insert a duplicate when it does, instead of relying solely on the caller's link-target-parsing presence scan. Measured scope, stated because the earlier claim was not scoped: for a target-breaking title, republishing an unchanged manifest converges on one row when the emitted child marker is `-`; editing that title between publishes can still leave one dead row per distinct link string, because the content-verbatim guard correctly treats each as a different row. Growth is bounded only by the number of title edits. When the emitted child marker is `*`/`+`, the same grouped link never reaches the file: the writer re-reads the proposed bytes, returns `unwritable`/`title`, and repeats that stable refusal on the unchanged index. The controlling marker is the new child's—reused from the last existing child, or from the container when it has none—not a property of the whole index. Other chapter-title and `group_title` faults may refuse before any row is written, as the adapters' measured rules describe. See "Nested-list automation limits" in either adapter.
- **One rule per question, across the index-wiring path (v1.13.0)** — three defects that could each put a wrong row into a real handbook index without saying so. The row locator and the nested-list writer now share ONE leading-frontmatter rule, so a stray backtick in a YAML scalar can no longer make a headings-form index read as absent *for the locator alone* and get a bullet-shaped container plus a duplicate row appended on every publish (#337). Container labels are compared under Unicode NFC at every comparison site, so a decomposed and a precomposed spelling of one title are one container rather than two pixel-identical ones (#351) — while an *invisible* character is refused instead of normalized — in the manifest, and in a nested-list index container, but not in a headings-form one (#553, below) — because no normalization merges two such labels and choosing one would be a guess; the manifest halt names the code point and its offset, "delete the invisible character" being the one instruction an operator cannot execute for a character they cannot see. And a placement verdict the container comparison fully supports is no longer discarded as `unverifiable` merely because the *label* would be unwritable (#350). Two adjacent gaps are stated in the adapters rather than left to be rediscovered: a headings-form container heading carrying an invisible character is not refused (#553), and a `group_title` carrying a line break passes every gate yet never resolves (#554).
- **The capture guard audits redirect hops, and its contract stops overstating itself (v1.13.0)** — a request the browser issues itself to follow a 3xx `Location` never reaches `context.route`, measured against real chromium on playwright-core 1.61.1 and 1.62.1, so such a hop was never classified, never recorded, and left `assertNoDangerousHits()` green. A second, audit-only channel re-classifies every hop through the same policy on the hop's own method, URL and body, exposes the chain via `redirectHops()`, and fails the run on a dangerous one — as **detection, not prevention**: the hop has already reached the server, and the failure says so rather than implying the request was stopped (#471). The mandatory pre-read `capture-spec-helpers.md` is corrected to what the policy actually enforces: interception covers what the engine surfaces to its handler, fail-closed covers non-GET/HEAD only, and a destructive GET fails closed only for a fixed 16-verb set — a GET to `/orders/42/confirm` is admitted. The origin-blind GET/HEAD allow (#470) and the same-origin `<iframe>` the scan does not reach (#472) stay open and are now stated rather than implied; the recurring-overstatement class itself is filed as #557.
- **Quoted labels are pinned at capture time (v1.14.0)** — page identity required only that the narrated element be asserted *visible*, which is a predicate about a box and not about text, so a spec anchored on a testid or a CSS selector satisfied it while pinning nothing the reader will read. Rename a column header and every gate stayed green while the published step quoted a label absent from its own screenshot. Step 4 now requires an assertion keyed to the exact quoted text and scoped narrowly enough to identify the one instance the step narrates — a page-wide exact match is satisfied by the same string anywhere on the page, and because the shipped helpers take the first match, scoping to the captured region is not yet identity when that string repeats inside it. The class that earns no coverage-matrix row is keyed on *inertness* rather than element type (a sortable header is an interactive trigger like any other), the self-audit carries the reciprocal obligation with disclosure prose as the stated per-label exception, and the shipped capture skeleton now demonstrates the pin instead of contradicting it (#477, #568). Also: `maskAndAssert` refuses a region that frames another document instead of only disclosing the carve-out, both skeletons stop letting a `finally` replace the primary failure, `capture.locale` is documented as pinning the process locale rather than the app's UI language, and the documentation claims measured wrong against the shipped code are corrected — including one in the `<iframe>` carve-out that justified an unqualified selector with a mechanism CSS attribute matching does not have; the changelog entry enumerates them (#472, #473, #474, #560, #563, #567).
- **A zero container match is not licence to create one (v1.15.0)** — both publish adapters turned `findContainer`'s `zero` outcome into an unconditional `## <group_title>` write, so a heading that merely *rendered* as the group title got a second, render-identical section appended beside it in an index the tool does not own; the run reported success and never self-corrected, because the next run matched the heading it had written itself. Both adapters now re-read the container headings first and halt when any plausibly renders as `group_title`, naming every candidate and prescribing no repair — whether two headings are one section or two is a question about meaning that a spelling comparison cannot reach. The fold covers ornament only, deliberately not a content-bearing suffix (`Reports (2024)` and `Reports (2025)` stay two sections), and a second detect-only compare strips the unsafe invisible characters and names the offending code point and offset. Also: the Related block names the handbook index as a legitimate third member with its target form in both link modes, and the `unverifiable` placement outcome now says who is left to look — the consuming agent reads the index region itself, which is not a confirmation step (#476, #553, #563 items 5 and 6, #345, #261).
- **A `<canvas>` is refused, not silently photographed (v1.16.0)** — `maskAndAssert`'s leak scan reads DOM text nodes, form-control values and placeholders, and what a `<canvas>` paints contributes to none of them — it is a bitmap, and only the canvas's *fallback* children are ordinary text nodes the scan sees. So canvas-rendered PII (a PDF.js document preview, a canvas-mode data grid, a signature pad) was composited into the PNG while the mask had nothing to overwrite and the scan had nothing of it to match. Measured in a real browser: the painted text never enters the scan corpus, and setting `textContent` on the `<canvas>` does not clear the pixels, so listing it in `selectors` is a no-op. The helper now counts every `<canvas>` in the region — light DOM, open shadow roots, and the region itself — and throws unless the caller passes `allowUnscannedCanvas: true`, the same shape as the existing nested-browsing-context refusal but with different remedies, since a canvas hosts no document to rescan. `maskAndAssert` also gains its first executable coverage, driving the real helper against a DOM stub.
- **The hand-written `.d.mts` files are checked against the code beside them (v1.16.0)** — nothing compiled them and nothing verified they still described their `.mjs`. A compile-free parity gate now compares each module against the REAL import namespace of its runtime file — names in both directions, declared arity as a range against `Function.prototype.length`, and orphan modules on either side including a declaration-only one a `*.mjs` walk cannot see. The declaration side is read by a statement-aware extractor rather than a regex, and any construct it cannot read fails the gate instead of being skipped. Declaration *type* correctness stays unchecked, since that needs a TypeScript toolchain this repository does not have (#420, #339; limits tracked in #573 and #577).
- **The publish-target extension contract says what a third adapter owes revalidation, addresses and completion (v1.17.0)** — `references/publish-targets/README.md` is the file a maintainer follows to add a target beyond the two that ship, and it asked which link *syntax* a target uses while never mentioning W6 at all, so a correct establishment-time adapter could leave the migration path undefined and still report a publish successful without verifying anything. It now carries three sections, derived by reading both shipped adapters and `chapter-paths.mjs` rather than a release diff. **Address derivation:** one formula per link class, each naming its origin, its operand normalization, its output and whether it is depth-sensitive — the shipped pair's own divergence is the evidence, since index→chapter is measured from the index file's directory by `static-md` and from the vault root by `obsidian-vault` under wikilinks, while chapter→asset is the same full-target join in both and should be mirrored rather than re-derived. **The completion gate:** shape fixed (ordered, halt on first failure, run after the write *and* after the wiring), item list not, with two items stated as target-specific because copying either shipped adapter is actively wrong — the two take opposite containment positions and their navigability floors differ for reasons that come from the renderer. Its membership item re-reads the index, because the wiring step returning success is not evidence the chapter is listed. **Revalidation and W6:** what a group change means in the target's own object model per delta kind, which changes an adapter may perform and which it must halt on, the halt's three-part structure, and the post-migration link scan. The contract also states plainly what no per-adapter extension point can express today — `manualMigrationChecklist` emits a fixed set of twelve fact kinds with no target parameter and no registry, `renderManualMigrationHalt` has no formatter hook, `migrationRecordPath` hardcodes the `.provenance` path (#380) — so a third adapter's revalidation contract is prose-only, and the document says so rather than leaving the next author to find out (#357).
- **The index rows a title edit leaves behind are named, not accumulated silently (v1.17.0)** — the membership guard added in v1.11.0 compares the *complete* current link string, so it bounds re-runs of an unchanged manifest entry and nothing more. Edit a chapter title in a way that breaks its own row's link-target parse — an unescaped `]` is the measured case — and the guard is handed a string it has never seen, correctly finds no match, and inserts: one unrecognizable row per distinct title, without limit, in both link modes (the issue scoped it to path mode; `[[handbook/admin/items|Items]v1]]` accumulates identically). New export `findStaleChapterRows` is a sibling of `locateChapterLine`, not a new writer outcome — the locator answers which rows *resolve* to the chapter, this answers which rows carry the same destination as a whole link token and still fail the parse, and the two sets are disjoint by construction, so `verifyNonHeadingPlacement`'s rule-4 accept-list needs no new member. Both shipped adapters call it at step 0 and halt naming every row with its line number and its raw text; neither deletes anything, because the index format carries no row-to-chapter ownership record and nothing distinguishes a row this tool wrote from one an operator hand-authored around an unparseable link. **Every one of the five admitted review findings on this change was in an *exemption*** — the logic deciding "this row is fine, do not name it" — and they are recorded because the pattern outlived each fix: own-row by containment (unsound against a title ending in the next title's opening bracket, measured at three accumulated rows and none reported); own-row by exact comparison, wrong on a CRLF index, where the writer reads `\r`-free logical lines; the same comparison wrong on any row shape other than a bullet, which halted on a numbered or table row the adapter itself had just appended and would append again — a false positive with *no edit that clears it*; a link-reference-definition exemption that matched a definition-looking *prefix*, so a title of `Old]: x y` hid its own leftover; and the destination match searching the raw target instead of the module's own `foldTargetForMatch`, which left one of the two accepted wikilink spellings unnamed. The resolution is an asymmetry rather than a better predicate: exact comparison on the `-`/`*`/`+` bullet the writer itself emits, containment everywhere else, because the two populations have opposite safe failure modes and an over-report is only cheap while it stays clearable. Two boundaries are stated rather than implied: a flat entry whose target-breaking title is never edited still appends one row per publish and this scan is blind to it by construction (#574), and the halt is what makes every exemption load-bearing in the first place (#585) (#349).
- **A flat index row is read back before it is written (v1.18.0)** — the other half of the row-growth defect, and the one no scan could reach. A manifest `title` whose text keeps its own row's link destination from parsing makes step 0 report the chapter absent on *every* run, so the flat "line absent ⇒ append" branch appends an identical row each publish while the manifest never changes: measured, four publishes leave four rows, in both link modes. `findStaleChapterRows` is blind to all of them by construction — each carries the link this run would write, which is precisely the test that tells this run's own row from a leftover — so widening the scan would have broken the unchanged-manifest case it exists to serve. Both adapters now compose the row, read it back through the very `locateChapterLine` call their own step 0 makes, and refuse to write one it cannot find, halting with the chapter's title as the thing to change. **The alternative was to recognise instead of refuse** — the flat analogue of the nested-list writer's `present` outcome, which the issue proposed: it bounds the count at one, but it leaves a permanently unreadable row in a managed file that every later run, migration and scan must special-case, so refusing is what ships. No executable behaviour and no export under `assets/lib/` changed — the only edit there is a comment on `findStaleChapterRows`; the rule is the adapters' and the extension contract binds a third adapter to the *obligation*, not to this spelling, because only that target's own reader can answer whether its own next run will find the row. **Two boundaries, both measured rather than argued:** which titles are refused is not summarizable by a character rule and neither mode is uniformly stricter — `Items]v1` and `A [b] c` are refused in both modes, `Items]` and `Items [beta]` are refused in path mode and accepted under wikilinks, and `Items \] esc` goes the other way — and rows already in an operator's index from before this release stay unreported while their title stays fixed, becoming visible to the #349 scan only once the refusal's halt leads the operator to change it (#574).
- **Sentences that cited closed issues as open work (v1.18.1)** — the 2026-08-18 tracker triage closed the extension contract's provenance-path item (#380), the doc-pin harness's structure-aware-reader item (#341) and the parity gate's unread-arity item (#577), each as a decision rather than as work delivered, while four sentences across `references/publish-targets/README.md`, `tests/reference-assets.test.sh` and `tests/export-parity-lib.mjs` still described them as tracked, filed or bounding. Each now states the decision and where it is recorded: the provenance record is a private audit trail for revalidation rather than a missing `publish.provenance_dir` key; semantic inversion by surrounding context is bounded by review alone, the structure-aware reader having been weighed and declined as disproportionate for a doc-pin harness; and the unread-arity residual is carried by the parity suite's own *every function export's declared arity was actually read* check, which goes red the moment one reaches the loop unread, rather than by a number nobody is working. **A citation is a claim with a silent failure mode** — a reader who follows it to a closed issue learns the sentence is stale, and a reader who does not keeps following a plan that no longer exists. One pin was added, on the provenance limit, so the next reader of that list states the decision instead of filing the key again. The scope is prose a reader executes or maintains today — dated release copy, here and in the changelog, keeps its own citations of since-closed issues, because each was true when it was published and this repository corrects forward rather than rewriting a dated entry. No behaviour, no export and no adapter rule changed.

### What it is **not**

- Developer / API / architecture docs — those belong in `CLAUDE.md`, `AGENTS.md`, or the project's internal knowledge area (e.g. an `obsidian-project-vault` wiki).
- A one-shot generator — it is a long-lived authoring loop maintained over the project's lifetime.

### Tips for best results

- **Plan first, then go wide.** In Claude Code, sketch the chapter plan before writing anything (plan mode), then drive authoring and review at high effort with multi-agent orchestration (e.g. `ultracode`) so several agents capture, cross-check, and validate coverage in parallel instead of one linear pass.
- **One page at a time.** Author and capture a single chapter per pass and keep its scope tight. A focused page is far easier to get right — and to verify — than a sprawling one; resist bundling unrelated features into one chapter.
- **Review from more than one perspective.** Have several agents read the drafted chapter, each from a different angle (a first-time user, a power user, a skeptic hunting for fabricated or undocumented behavior). More viewpoints beat one — no single pass catches everything.
- **Rerun and validate coverage.** When a chapter (or the whole handbook) is done, run the skill again as a completeness pass: walk the actual feature surface and confirm every feature is described. The first pass always misses some.

## `literary-translator` — v1.62.0

High-fidelity literary **book translation** over a Gutenberg-style EPUB source (or, in expert mode, a hand-co-designed `custom` extractor for any other source shape; a `plain_text` adapter is specified but not yet implemented, #62). A `codex-translate → deterministic false-green gate → codex-review → Claude-fix` loop runs to convergence per segment / per novella — never per book — with a frozen name/realia **canon**, a configurable **verse policy**, and **ledger-based resumability**. The canon freeze is one-way, so under `live` research mode a glossary batch's source citations are reviewed *before* it is allowed to merge, while its fragment is still rewritable. Once every in-scope segment converges, the drafts optionally assemble into an Obsidian glossary-wiki (keyed on the frozen canon) behind a deterministic render/diff acceptance gate.

Trigger phrases: "translate this book", "set up a literary translation pipeline", "new book translation project", "translate this EPUB/story collection from X to Y", "Gutenberg EPUB translation", "resume book translation" — full list in `plugins/literary-translator/skills/literary-translator/SKILL.md`.

### What it covers

- **Engine loop** — per segment: codex translates, a deterministic false-green gate (`validate_draft.py`) rejects placeholder / empty / policy-violating drafts, codex reviews, Claude applies fixes, looped until converged. The scripts surface candidates and enforce schemas; the accuracy / identity calls are codex's, never a script's.
- **Frozen name/realia canon** — a 1:1 `source_form → canonical_target_form` dictionary (`canon.json`) with a validation gate (`canon_validate.py`) and an opt-in human-adjudication gate (`canon_adjudication_audit.py`) that turns duplicate / merge / missed-pair / unresolved-queue review requirements into a persisted, machine-checkable record.
- **Homonym splits reach the translator (v1.45.0)** — a source form denoting two or more distinct referents cannot be one frozen canon entry, so it is adjudicated in the `canon_senses.json` sidecar; the per-segment input now carries those senses as `split_names`, each with its disambiguator, instead of filing the form as an unresolved new name. There is no frozen target form at a split name, and the translate/review/fix prompts say so in both directions: pick the sense the passage carries, and never prescribe a canonical form there.
- **Verse policy** — configurable handling of verse vs prose (`rendered` / `literal_gloss` fields, per-mode validation).
- **Ledger-based resumability** — a `ledger.json` with a composite cache key so an interrupted run resumes safely and re-applies any style-bible / canon edit rather than shipping stale drafts.
- **The source itself can be mangled, and no gate could say so (v1.46.0)** — a PDF-converted EPUB can carry RTL text in *visual* order rather than logical order. Extraction preserves those bytes faithfully, which is correct, and every deterministic gate then passes them, because token counts, digests and schema validation never read what a fragment means. The cost lands on the model turns: torn tokens draw reviewer findings against correct drafts, and one such passage reached a converged draft with its subject and object swapped. The post-extraction gate now prints a report-only advisory — it cannot refuse a book or rescue a failing extraction — with escaped, codepoint-level evidence, because a bidi terminal renders a corrupted RTL token exactly like an intact one. It is a screen, not a verdict: a reader adjudicates the sample, and on a positive the condition is recorded where the translate, review and fix turns all read it.
- **A style-contract edit that doesn't cost the book (v1.41.0)** — the style contract is a cache-key input, so appending one rule flips every already-converged segment to `stale` and both ship gates then refuse until each is reviewed again. The shipped rule says such an edit applies *forward* and owes no back-sweep; `validation.admit_contract_only_stale` lets the gates agree, admitting a flipped unit whose draft is unchanged since review and whose only non-machinery cache-key move is the contract itself. Off by default, it rewrites no ledger record and stamps no hash, and every admitted segment is named in each gate's output — because after a rule *reversal* (as opposed to an addition, which no hash can distinguish) re-reviewing is still the right answer.
- **Source adapters** — `gutenberg_epub` (the one working built-in adapter) plus an expert-mode `custom` extractor (supported, experimental); `plain_text` is specified but not yet implemented (#62). Scripts are self-anchored and stdlib-first; each emits one JSON line to stdout, human detail to stderr, exit 0 / 1 / 2.
- **Book assembly + output rendering (v1.1)** — once every in-scope segment is converged, `assemble.py` joins the manifest + per-segment drafts + verse map (ledger-gated on `converged` + sha1 match) into a target-agnostic NodeStream, and an output adapter renders it. The shipped `obsidian` adapter produces a vault of chapter notes with folder-qualified `[[wikilinks]]`, footnotes, and verse blocks, plus one entity note per canon entry (canon IS the entity registry). `diff_rendered_output.py` is a deterministic render/diff acceptance gate (`--accept-baseline`, then re-render must match). All fail-closed against symlink data-loss.
- **Workflow-orchestration reliability (v1.2)** — review and the glossary-pass batch call now follow translate's proven dispatch → bounded-poll → disk-read pattern instead of a bare, unbounded codex call, closing a hang risk a real 11-teammate incident on the source project already surfaced. Every Workflow-tool agent schema is a flat, tool-use-API-legal top-level object (a top-level `oneOf`/`array` schema simply cannot be an `agent()` schema — the earlier shape blocked review and glossary dispatch outright). Concurrent glossary batches write to their own run-scoped fragment instead of racing on a shared `canon.json`, with one serialized merge plus an independent disk-verification pass before the pass is trusted complete. A run-scoped token on every fire-and-forget artifact, checked at every point it's consumed or committed, closes a class of stale-artifact-from-an-interrupted-run bugs; whether to resume a run at all is gated by a dedicated input/version digest.
- **Verse×footnote correctness cluster (v1.3)** — the extractor now handles poems whose stanzas lack `.line` children and verses nested in heading-wrapping `<div>`s; footnotes cited inside a verse are recorded and carried through segpack/validate/assemble/render instead of being dropped or left as a dangling definition; a shared verse×footnote fixture corpus exercises the full chain end-to-end.
- **Authoring-gate hardening + doc-prose leak fix (v1.3.1)** — `scaffold_validate.py`'s W1 gate now rejects unfilled bracket placeholders (`[SOURCE LANGUAGE]`, `[PROJECT TITLE / AUTHOR / PERIOD -- fill in]`, ...) in hand-adapted planning docs via a closed-list, whitespace-normalized scan, without risk of blocking legitimate editorial brackets like `[NOTE]`/`[SIC]`; the companion ERA/DOMAIN trap-string check gained a co-occurrence-based scan catching mangled/partial trap examples; and two reference docs no longer instruct the reader to read a non-shipped internal path directly, with the drift guard extended to scan doc prose as well as scripts.
- **Bugfix release (v1.3.2)** — the `select_segments.py` regen hint for a stale `derivation_bundle_hash` now names the correct steps (`bootstrap_names.py` and the glossary pass, not `segpack.py`, which only ever copies the hash forward); the language smoke gate strips `⟦FNREF_N⟧`/`⟦VERSE_…⟧` sentinels before candidate extraction and density scoring so they can no longer corrupt or false-fail the gate; and `canon_validate.py` now rejects (both at merge time and at `--verify-merged`) a `source_form` present in both `entries{}` and `review_queue[]`.
- **Output-layer polish + first-run robustness (v1.3.3)** — the render/diff acceptance gate's baseline reader now mirrors the writer's exact newline-splitting semantics, so a rendered line containing a Unicode line-boundary character (U+2028/U+2029/NEL/...) no longer false-mismatches forever; entity-note filenames are de-duplicated on NFC-normalized casefold, closing a silent note-clobber on case-/normalization-insensitive filesystems (macOS APFS, Windows); `assemble.py`'s one runtime-evaluated PEP-604 union is now a quoted forward reference (with a permanent AST-based drift guard against future regressions), fixing a hard crash on Python ≤3.9; `render_obsidian.py`'s `import yaml` now preflights like every sibling script instead of raw-tracebacking on a missing dependency; the foreign-remainder stopword check no longer silently no-ops on punctuation-adjacent tokens; and a name appearing in both an inline verse and its host prose no longer renders two wikilinks.
- **Verse×footnote correctness cluster, round 2 (v1.3.4)** — `render_obsidian.py` no longer renders only the first entry of a multi-verse `kind:"verse"` node (a silent whole-content data-loss bug, #119); under `verse_policy.mode: skip`, a footnote whose only citation site is a mode-voided verse's content no longer deadlocks whole-book assembly (`orphan_footnote_def`), with the exemption driven by the manifest's mode-independent `verse.store` ground truth and extended so a verse embedded in the exempted footnote's own definition isn't then false-orphaned (#118 item 1); a footnote cited inside a verse that is itself embedded in another footnote's definition — to arbitrary nesting depth — is now discovered by a segpack worklist/fixed-point and a de-duplicated recursive assemble helper, referenced-only so nothing dangles (#118 item 2); and an embedded verse that is the entire content of a prose block now renders as a blockquote instead of inline italic (#118 item 3).
- **W3 glossary-pass resumability, cost guardrails & elision adjudication (v1.3.5)** — a new `glossary_batch_plan.py` owns candidate→batch curation: it excludes any candidate already in canon `entries{}` **or** `review_queue` (with an explicit `--retry` override) before re-batching, so a resumed glossary pass no longer re-researches names a prior run already queued (#101), and emits a `no_new_candidates` marker that skips the pass entirely when nothing is left to do. The glossary-pass Workflow gained a preflight cost cap (`3·batches + 2` estimated calls vs `engine.batch_agent_cap`), and the shipped `batch_agent_cap` default was recalibrated 1000→3500 so a normal-length novel is admitted rather than refused on the first run (#95). A capitalized elided article that fuses onto a name (`L'Enclos` where `Enclos` is itself a candidate) is now detected in `bootstrap_names.py` and flagged `elision_ambiguous` for the glossary adjudicator to route to `review_queue` — resolving the ambiguity at the adjudication stage instead of widening `ELISION_RE`, which a prior attempt showed re-breaks fixed compounds like `D'Artagnan`/`L'Aquila` (#91).
- **First-scaffold convergence blocker + glossary & typing fixes (v1.3.6)** — `style_bible.template.md` now ships the `STYLE_CONTRACT_BEGIN`/`STYLE_CONTRACT_END` markers wrapping style_contract sections A–F that `cache_key.py`'s `compute_style_contract_hash` hard-requires; without them every freshly scaffolded project translated and reviewed cleanly to disk but recorded "0 converged" (`ledger-write-failed`) on every segment — an opaque hard blocker whose root cause was two missing comment lines, now also caught early by a fourth `scaffold_validate.py` W1 gate that mirrors the hash consumer's exact marker byte-strings and failure conditions (#129). The glossary pass gained a canonicalization rule preventing a salon nickname/epithet/alias from inheriting its referent's real-name `canonical_target_form` — only true orthographic variants of one surface name may share a form (#134). And `extract.py.template`'s fatal-abort helpers (`_missing_dep`, `die`) are annotated `-> NoReturn`, silencing spurious Pyright "possibly unbound" warnings on the optional-dependency imports (#136).
- **Canon enforcement, transient recovery & review-gate correctness (v1.3.7)** — the frozen `canonical_target_form` now actually reaches the translate/review prompts via a new segpack `canon_map` (with a declined-stem rule so a correctly inflected canonical name is never flagged); transient/mechanical mass-translate failures (poll timeouts, a fix call that dies / hits the output-token ceiling / is classifier-blocked on a valid draft) become recoverable instead of human-escalated; the review-artifact compare no longer terminal-blocks on a free-text transcription slip; and an infra-fabricated sentinel-`loc` review verdict is rejected before it false-blocks a clean draft.
- **Fifth canon `basis` value, `sense_translated` (v1.4.0)** — a speaking / meaningful name rendered by sense rather than transliteration is now lockable in the frozen canon with a `canonical_target_form` (previously such names were re-parked in the review queue every run), guarded on mid-pipeline resume by a new `glossary_preflight.py` staleness gate that halts before dispatch if a project's durable schema copy predates the 1.4.0 contract.
- **Documentation-and-gate hardening (v1.4.1)** — the `plain_text` source-adapter fiction is reconciled across every reference doc, marketing description, and code comment that presented it as a working/shipped adapter (it remains specified but not yet implemented, #62); the W3 language-smoke `pass:true` framing is corrected to be honest about uncased-script (Hebrew/Yiddish/Arabic) blind spots, and its count-based "completeness" label is renamed to what it actually enforces (an entry-count, dedup-blind floor); and `validate_draft.py`/`draft_ready.py` gain a draft `seg`-identity check that rejects a mislabeled/cross-wired draft instead of certifying it `OK`/`READY`.
- **Render/validation placeholder-fidelity cluster (v1.4.2)** — `validate_draft.py`'s placeholder gate no longer assumes a `VERSE_` prefix (a custom adapter's embedded-verse placeholder such as `⟦POEM_1⟧` is now matched by an exact map of the segpack's declared placeholders plus footnote anchors, closing a false-green where a dropped placeholder passed validation); segment-note titles and filenames no longer leak a raw `⟦…⟧` sentinel out of a heading; and multi-line footnote definitions and verse literal-glosses no longer eject their continuation lines out of the footnote/blockquote (CRLF/CR/LF line endings normalized).
- **Caseless surfacing + homonym-split sidecar — RFC #215 Phase 0+1 (v1.5.0)** — caseless multiword surfacing for unicameral scripts (`bootstrap_names.py` two-pass offset-preserving extraction plus a new `occ_index.py` source-occurrence index), and a strict, byte-verified `canon_senses.json` homonym-split sidecar (≥2 senses per split form) enforced by a mandatory category-5 split-evidence audit gate; `canon.json` stays a 1:1 dict.
- **Structural-risk triage + adverse-only skeptic pass — RFC #215 Phase 2 (v1.6.0)** — a deterministic, confidence-independent `suspicion_scan.py` surfaces structurally-risky canon entries (over-merge, citation-only, near-duplicate, high-dispersion, offline-established, sampled), and a scoped, source-grounded skeptic codex pass re-examines them against bounded per-entity windows, able **only** to raise adverse / propose-split / propose-rescope blockers *with byte-verified evidence* into a separate triage artifact no freeze/merge reader opens (the category-5 audit gate is unchanged). Disabled by default; the warn→block flip is deferred to a later phase.
- **Assemble/audit delivery-gate hardening (v1.7.0)** — `final_audit.py`'s exit code is now a fail-closed completeness signal (`0` clean / `1` hard defects / `3` project incomplete) on the default delivery path, matching the same `project_complete` predicate `assemble.py` already enforces on the render path (#208). A manifest may now declare `heading_types`, so a custom extractor's own heading block-type tags classify as headings instead of rendering as flat prose under a raw seg-id title/filename — absent, behavior is byte-identical to before (#210). A new `validate_assembled.py` runs the union structural-completeness invariant over the declared-heading set — every source `(seg, block_id)` heading marker must surface as a non-empty heading — in both the default scope (rebinding every draft read to the ledger's `reviewed_draft_sha1`) and the `assembled_book` render scope, with a non-gating WARN for an undeclared heading-like block (#202).
- **Hebrew / uncased-script enablement + scaffold & output-contract hardening (v1.9.0)** — an offset-safe, mark-inclusive `TOKEN_RE` keeps a pointed/vocalized word (Hebrew niqqud, Arabic harakat, Latin NFD accents) a single token with its source offsets preserved, instead of shattering it one token per base letter — the offsets `occ_index.py`'s evidence spans bind to; Hebrew geresh/gershayim/maqaf name-connecting punctuation is likewise kept intra-token so an inventory name binds back to its source spelling (#225). A shipped `he.json` starter preset for the uncased Hebrew script (#195). A Hebrew-scoped niqqud fold so `final_audit.py`'s foreign-remainder check matches a pointed draft token against its unpointed stopword (#209). `scaffold_setup.py`, Step 0a's shipped writer for the `.plugin_bundle_hash` / `.orchestration_bundle_hash` markers a real run needs (previously prose-only, so a run failed "has Step 0a run for this project?", #194). And an enforced heading-shape output contract — `validate_assembled.py` rejects a translated heading whose text begins with a literal markdown `#`, false-RED-averse (#201). The #225 tokenizer change re-derives name candidates and stales the W3 smoke report for existing projects (both unavoidable; NFC-Latin output is byte-for-byte unchanged).
- **Fold-aware evidence chain + conservation gate + third-language slot (v1.11.0)** — the evidence/adjudication chain is now Hebrew mark/connector-insensitive like the `## Mentions` appendix matcher, so the two agree on what counts as the same name instead of the appendix folding while the evidence chain stayed literal (#243); exact-match sentinel comparison replaces substring checks across the two remaining workflow templates, closing a class where a healthy fragment could be terminally blocked or a draft-missing fragment silently merged (#228); a new `validate_conservation.py` content-conservation gate guards the manual PDF→EPUB wrap against dropped or gutted source blocks (#196, and #202's output-coverage half); and `style_bible.template.md` gains a required fill-slot for embedded third-language text (e.g. a Yiddish phrase inside a Hebrew book) so its romanize/translate/gloss convention is fixed before translation instead of drifting across incompatible forms (#203).
- **Configurable codex effort + optional model pin (v1.12.0)** — codex reasoning `effort` becomes a real per-project `engine.effort` enum (`low | medium | high | xhigh`, default `high`), actually threaded into the W5 translate/review dispatch, the Claude fix step, and the glossary pass instead of being schema-pinned to `high` and never reaching codex; an optional `engine.model` pins a codex model id for the W5 dispatch. Both fold into the run's cache/resume identity so a re-run at a different `(model, effort)` no longer silently reuses artifacts (#197).
- **Occurrence-index close-out — homonym de-link + orphaned-homonym diagnostic (v1.13.0)** — the inline wikilinker no longer misattributes a homonym: a `canonical_target_form` shared by ≥2 canon entries is de-linked on every Obsidian render regardless of the `## Mentions` appendix `enabled` flag (previously an `enabled: false` opt-out reintroduced the shortest-`source_form` tiebreak that could pick the wrong entity), while the default-on source-anchored `## Mentions` appendix stays the authoritative, variant-immune, homonym-split occurrence index (#207); `validate_backlinks.py` gains an exit-neutral `collisions[].orphaned_owners` diagnostic that surfaces a collision owner the renderer inline-links nowhere and that has no `## Mentions` backlink either; and the stale "native backlinks are the occurrence index" documentation is reconciled to name the appendix as authoritative and native inline backlinks as a best-effort, verbatim-same-surface reading affordance (#206).

- **Consume-site correctness + a sanctioned regeneration escape (v1.15.0)** — found by the plugin's first live end-to-end W5 run. Three workflow guards tested for the PRESENCE of an optional field where they meant to test for EVIDENCE of failure, so an agent's truthful `exit_code: 0` was read as proof of failure and converged, already-written segments were reported `ledger-write-failed`; all three now route through one benign-value table, and the review-artifact guard additionally gained the allowed-key check it never had (an undeclared key used to pass as a match). `canon_validate.py --init` bootstraps the empty-but-stamped `canon.json` that the documented zero-candidate SKIP path — the route every uncased-script project takes — previously left uncreated, dead-ending W3a at `FATAL: canon.json not found`. A merge that changes nothing no longer advances `canon.json`'s `generation_hashes`, closing a route that cleared `blocked_needs_regeneration` with nothing regenerated; the sanctioned replacement is a new `canon_validate.py --restamp-derivation`, which `select_segments.py`'s blocked-state hint now names for both glossary-pass-routed fields, with the full blocked → restamp → segpack → cleared recovery pinned end to end against the real scripts (#289, #290, #291, #292, #193).
- **Line-oriented sentinel verdicts for wait/precheck replies (v1.15.1)** — a wait/precheck agent's reply that decorated a mandated bare sentinel (`READY <seg>`, `PRESENT <i>`, `DRAFT_MISSING <seg>`) with prose was rejected by the previous whole-string exact match, mislabeling a genuinely completed review/translate/batch as a timeout. All seven consume sites across the three workflow templates now accept a reply whose own last non-empty line is the sentinel, while rejecting any reply one of whose own lines *equals* the failure sentinel — every line position is scanned, but whole trimmed lines are compared, never substrings — and any reply that quotes then disavows the sentinel on a non-final line (#308). *Corrected in v1.16.0: this bullet used to claim the check rejected any reply **containing** the failure sentinel anywhere. It never did — a failure sentinel sharing a line with prose was simply not seen. The description was stronger than the implementation, and the gap outlived a full release partly because the sentence read as reassuring: the same defect class — a check described more strongly than it is written — that v1.16.0's containment guard exists to close, and which finally makes the stronger claim true.*
- **Hebrew ASCII-punctuation connector equivalence (v1.15.2)** — a Hebrew acronym or compound written with the ASCII quote/hyphen/apostrophe real corpora actually use (e.g. `מוהרנ"ת`, `הבעל-שם-טוב`) now surfaces and matches inventory spellings written with the dedicated gershayim/maqaf/geresh glyphs or plain spaces, closing two related tokenizer/fold gaps that silently dropped candidates (#282, #283).
- **Pre-merge citation review in the glossary pass (v1.16.0)** — under `glossary.research_mode: live` the pass resolves some names as `basis: "established"` with a `source` URI, and nothing reviewed those citations before they were frozen: the batch's own self-check proves shape, offline backstop and candidate coverage, never whether a cited source supports the resolution attached to it, so an unreviewed citation merged into `canon.json` under a clean, disk-verified pass. A bounded citation review now gates a batch becoming READY, and it runs pre-merge because every post-merge surface can report the problem and none can repair it — `--verify-merged` writes nothing, a re-merge of a different resolution for the same `source_form` is rejected as an `entries{}` collision, and `canon_adjudication_audit.py` only blocks. A rejected citation regenerates that batch's fragment to a fresh attempt-scoped path before any merge, bounded by `MAX_CITATION_RETRIES`; exhausting the bound returns a distinct `citation-review-exhausted` no-merge result rather than merging anyway. No-op under `offline`, where `basis: "established"` is forbidden outright.
- **Sentinel containment guard across six verdict sites (v1.16.0)** — the false-GREEN dual of #308, and what makes the v1.15.1 bullet above finally true. `sentinelVerdict()` compares whole trimmed **lines**, so a failure sentinel sharing its line with anything `trim()` does not strip was never seen, and a trailing clean success line then approved the work anyway — a reply carrying *both* verdicts silently resolving to the approving one. Measured against the shipped function, each count named with its shape and its set because the same guard over a different table or reply shape yields a different and equally correct number: **15 of 16 over `GLUE_CHARS` in `tests/glossary_citation_review.test.py`, prose sharing the sentinel's line** (7 of 16 with the sentinel alone on its line, where `trim()` rescues 9 — note U+2028/U+2029 are stripped but U+0085 is not, so the set cannot be judged by eye), and **14 of 15 over `ALL_GLUES` in `tests/mass_translate_sentinel_containment.test.py`, prose sharing the sentinel's line** (6 of 15 alone). Five sites now short-circuit to REJECT on `rejectedAnywhere()` substring containment, evaluated before the line check: the glossary pass's resume precheck, readiness wait and citation review, plus mass-translate's translate and review waits. Containment is strictly easier to satisfy than line equality, so the guard can only add rejections. A sixth site — mass-translate's `DRAFT_MISSING` fix check — runs the same test in the **opposite** direction via `mentionedAnywhere()`: there the sentinel is the success one, so gluing hid a genuine missing-draft report and the loop carried on reviewing a draft the fix agent had just called absent; the accepted cost is that a reply merely *mentioning* it now probes and returns the non-terminal `fix-call-failed`, which auto-redispatches. `skeptic-pass-wf.template.js` is deliberately left unguarded — it belongs to no `cache_key.py` bundle and carries its own resume digest, so editing it would mint a fresh skeptic RUN_ID this release does not otherwise pay.
- **The W5 wait is spent across calls, and a finished artifact is re-read (v1.16.1)** — `WAIT_BOUND_SEC` (2700 + 150 + 600 = 3450 s) was polled inside a *single* `agent()` call, but the agent's Bash tool clamps one call at 600 000 ms regardless of the timeout requested. Measured, not inferred: the failing call asked for `timeout: 3600000` and returned `Exit code 143 / Command timed out after 10m 0s`, so "raise the timeout" does not exist as a fix. Observed on the 1.16.0 release gate, one segment's three waits: 511 s → READY, 311 s → READY, **611 s → TIMEOUT** — the last leaving a complete, schema-valid `seg03.review.json` on disk beside a ledger reading `in_progress`. Across that whole run the correlation is exact: of 10 polling waits, the one that exceeded 600 s is the only one that failed, and every wait under the clamp returned READY. The wait now spends its bound across 8 bounded chunk calls, each polling whatever is LEFT of the bound (chunks 1–7 at 480 s, chunk 8 at 90 s, summing to exactly 3450 s — flat chunks would have silently *extended* the declared bound to 3840 s rather than spending it). Chunking alone would have turned that 611 s failure into a success by accident while leaving the real hole open, so the actual fix is the **authoritative re-check**: after the chunk budget is exhausted, or a chunk reports the driver's fail sentinel, one non-polling call runs the same canonical gate once more before any timeout is declared. Both paths share a single ACCEPT builder per site, so the re-check can never drift into a weaker gate than the poll it backs up — that drift would be a false GREEN. Wait replies use a three-sentinel grammar (`READY`/`FAILED`/`PENDING`; `TIMEOUT` is gone from these two sites) parsed in exactly one place, with both containment guards still running before the whole-line READY test, so every #228/#308 property is preserved and anything unparseable is PENDING, never READY. Blocked reason strings (`translate-timeout`/`review-timeout`) are deliberately unchanged — recovery keys off the absence of a terminal ledger write, not off the strings. Evidence at its true width: the targeted proof is `tests/wait_chunking.test.py` (16 tests, watched failing before the fix); the 1.16.1 release gate independently exercised one real chunk-boundary crossing (chunk 1/8 PENDING at its 480 s bound → chunk 2/8 READY), but at ≈543 s total that wait sat *under* the 600 s clamp, so it demonstrates the handoff works rather than reproducing the failure. **Scope stated rather than implied: this fixes the W5 mass-translate waits only.** The glossary pass and the skeptic pass still poll `45 × (check + sleep 20)` — about 900 s — inside a single `agent()` call, which is the same defect class over the same 600 s clamp, and neither re-checks the artifact on a failed reply. They are untouched here and tracked as #352; a release that closed the class everywhere would have had to say so, and this one does not. Cost: a wait is now up to 9 calls instead of 1, moving the per-segment worst case from 38 to 86 at the shipped `max_fix_rounds: 4`, so a batch at `batch_agent_cap: 3500` admits at most 40 segments — refused at preflight before any work starts, never mid-run (#348).
- **Citation retrieval happens only through a validated boundary (v1.16.1)** — the W3 citation reviewer added in v1.16.0 fetched each `source` URL itself with no scheme or address validation at all. A `source` is attacker-influenced in the only sense that matters — an LLM produces it from source text a hostile document can seed — so `http://169.254.169.254/latest/meta-data/`, `http://127.0.0.1:6379/` and `file:///etc/passwd` were all reachable from a "citation". New shipped script `fetch_citation.py` (standard library only) is now the sole sanctioned retrieval: scheme allowlist, rejection of embedded credentials and control characters, `localhost`/`*.localhost` refused by name, and **every** address `getaddrinfo` returns checked for global-ness — not just the first, which would leave a trivially winnable race. It connects to the resolved IP while TLS SNI stays bound to the original hostname, closing DNS rebinding without trading an SSRF hole for a MITM one; redirects are followed manually with every hop re-validated from scratch, capped at 5, with a malformed `Location` refused rather than raised, under caps on response bytes, content type, and two separate time budgets — a per-item 30 s bound re-checked at every redirect hop *and between body chunks* (one documented gap remains: `getaddrinfo` takes no timeout argument) plus a batch-wide 420 s budget, because 40 sources × 30 s inside one Bash call would exceed the same measured 600 s clamp #348 is about. Telling the reviewer to fetch "only through the helper" was tried and **rejected** during review: the reviewer is an unrestricted agent that already holds Bash and already ingests page content, so a hostile page can simply instruct it to fetch something else — *a rule the attacker can talk the enforcer out of is not an enforcement point*. Retrieval therefore moved out of the judging agent, splitting the review into a **prepare** agent that runs the fetcher and reads only the single locally-generated metadata line it prints, and a **judge** agent that reads local files only, performs no retrieval, and is handed no mutable fragment path anywhere in its prompt. Precisely, and narrower than an earlier draft of this bullet claimed: the judge is given no retrieval instruction and no fragment path, but it *does* receive URLs — `index.json`'s `source` field is the cited URL itself, and a fetched body can contain any URL at all. It is still an ordinary agent holding Bash. What the split removes is the *reason* to fetch and the *provenance* of every byte it judges, not URLs and not the tool; its prompt marks those fields untrusted explicitly. No server-supplied *free text* reaches the file the judge is told is locally generated, and every field still carrying a server-chosen value is named untrusted in the judge's own prompt — a claim it took five review rounds to state at the right width, each round closing one channel and the next finding the sibling it missed. The absolute version of that sentence was false while it stood: a redirect lets the server pick the next hop's host, so `final_origin` and `chain[].host/origin` carry an attacker-authored hostname by design, bounded to a hostname's shape and kept because an operator diagnosing a citation needs it. A hostile `Content-Type` used to land verbatim in `index.json`'s `outcome`, exactly the field the judge reasons over, so content types are now collapsed to a closed token set (the configured allowlist plus `absent` and `other`). Round 2 found the same class in the sibling *fields*: `final_url` and `chain[].url` carried server-authored path/query/fragment text, now reduced to `final_origin` plus `{origin, host, hop, resolved}`. Round 3 found it in the *parser*, which is not a field at all — `http.client.HTTPException` is not an `OSError`, so a malformed status line escaped every handler and printed a traceback whose text is the server's own (`BadStatusLine` stores the raw wire line in `args`), straight to the one agent the split exists to insulate; only the stdlib type name crosses now. Round 4 stopped enumerating and made the boundary *total* — any exception that is not already a refusal becomes one carrying the stdlib type name and nothing else. Round 5 then found the sixth instance where no guard on this file could reach it: `canon_validate.py`, the twin that runs the same static decision offline with no resolver behind it, still echoed a raw URL scheme into its refusal reason, and the parity test covering the two had only ever exercised schemes on which they agree by construction. The same round bounded the body read against a server that trickles (one held socket could otherwise run the whole prepare call past the 600 s clamp and merge zero batches) and refused IPv6 site-local `fec0::/10`, which both halves had admitted because CPython reports it `is_global`. Rounds 6 through 8 each found the defect in the round before it, which is recorded here rather than smoothed over because the pattern is the point: round 5's deadline fix bounded the body read and not the three phases that block *inside* a single stdlib call (measured, 24.1 s against a 3 s deadline), and two plausible repairs — checking the clock between calls, then re-arming the socket timeout — bounded nothing before an out-of-band watchdog did; round 6's numeric-host refusal checked the bytes in the URL while the resolver checks the IDNA-folded form, so seven Unicode spellings passed both static halves, one of them reaching cloud metadata; and round 7's cap on diagnostic output covered the two sites that had been measured while three siblings still emitted 563 KB at the shipped batch size. Each repair moved the guard somewhere it cannot be missed — a watchdog rather than a clock check, the fold applied to every host-shaped test, the bound inside the exception every failure passes through — rather than closing one more instance. The admitted content types are also configurable per project via `glossary.citation_content_types`, for corpora that legitimately cite scanned archives. Offline, where nothing ever fetches, `canon_validate.py --check-batch` statically refuses an unsafe `source` with no DNS and no network — applied to every item whose `source` is a non-empty string, not only `basis: "established"` ones, because the queued branch of `canon-batch.schema.json` types `source` as a bare unconstrained string. (An empty or non-string `source` is skipped deliberately and identically in both files: it is not a fetch target, and its shape is Pass 1's business. The two must agree on *which* items they cover, not merely on the checks they run — a divergence there is a hole neither file's tests would show, which is also why one trailing DNS root dot is now stripped before the `localhost` name test in both, closing a static false-negative the fetcher caught after resolution and the offline path, having no resolver, did not catch at all.) `fetch_citation.py` joins `PLUGIN_BUNDLE_MEMBERS`, without which editing the security boundary would move no hash at all; `resume_setup.py` now wipes stale citation evidence **directories**, which the existing fragment regex could not see and `unlink()` could not have removed. The claim at exactly the width it is true: *in the citation audit path, retrieval happens only through `fetch_citation.py`, launched by an agent that never reads the retrieved bytes.* It does **not** make the pipeline SSRF-free — the resolver agent still does open web research by design under `research_mode: live`, and the judge still holds a Bash tool; both are named rather than quietly covered, and tracked as #353. Cost: the live glossary ladder moves from `10*BATCHES.length + 2` to `13*BATCHES.length + 2`; the offline ladder is byte-identical, so no offline project's tuned cap starts refusing (#347).
- **The glossary and skeptic waits are spent across calls too (v1.16.2)** — v1.16.1 fixed the W5 wait and said, in its own notes, that these two still polled `45 × (check + sleep 20)` — about 900 s — inside a single `agent()` call against the same measured 600 s Bash clamp, tracked as #352. This closes it by porting the v1.16.1 pattern rather than inventing a second one: two bounded chunks of 480 s and 420 s summing to exactly 900 (flat chunks would silently *extend* the declared bound rather than spend it), then one authoritative NON-polling re-check before any timeout is declared, so a fragment that becomes valid between two polls usually is no longer reported as a timeout while sitting complete on disk — with one residual, inherited from the same guard that makes the rest of it true: the re-check's own reply is read by the same guarded `waitChunkVerdict()`, so a reply that merely mentions the batch's `PENDING` sentinel in prose (recapping an earlier chunk's verdict, say) while also stating `READY` on its own line still resolves to `pending`, false-RED by the same deliberately one-sided bias, and here with no later call left to recover it. `READY` in any chunk ends the wait immediately — which matters beyond tidiness here, because `skeptic_ready.py --validate-fragment` normalizes in place and is **not** idempotent, and the normal path already runs it twice — codex's own self-check, then the wait poll — so an extra chunk after a `READY` would add a **third** write-capable validation, not a second. The cost is operator-visible and moves three refusal thresholds, not one: a wait is worth **up to** `WAIT_CALLS = 3` calls instead of exactly 1, so at `batch_agent_cap: 3500` the live glossary gate admits 269 → 184 batches and both `3N+2` gates 1166 → 699. Offline is charged the same ceiling deliberately, reversing a shipped comment that argued for holding it byte-identical: charging offline for a retry ladder it can never enter would be a false refusal, but these wait calls are work an offline run genuinely can spend. **The review round then found the release's own defects, which is why they are listed rather than smoothed over:** the containment guard had been ported to the wait site and not to the resume precheck one line away (measured against the shipped functions over the shared `GLUE_CHARS` set, 16 items: 15 of 16 glue characters made the unguarded skeptic precheck resume-skip what the glossary twin would regenerate, the same false-GREEN direction and the same set the wait site's own guard already closed), and the parity test that existed to catch exactly that compared helper *definitions* while asserting something about *call sites* — mutation-proved green with both skeptic delegations bypassed. The gate now enumerates call sites out of each template with a hand-built tokenizer (not a full parser) rather than a regex over source, pinning exact per-template counts plus an exact total so an enumeration matching nothing cannot read as clean. Four further statements were corrected for being stronger than the code, including one in a file outside the release diff that no diff-scoped review could reach. Deliberately **not** fixed and **not** filed: `sentinelVerdict()` still accepts a disavowal followed by a quoted final sentinel — measured identical at the pre-release baseline, so it is the parser's semantics since v1.15.1 rather than a regression, and tightening it re-opens the #308 failure it exists to prevent (#352).
- **A real trust boundary for codex jobs (v1.17.0)** — the gate scripts that validate a codex job's output lived inside the directory that job can write: `codex_job.py` launches the companion with `--write` over the durable root, and Step 0a copies `assets/scripts/*.py` into `${durable_root}/scripts/`. Four successive by-path guards were designed against this during planning and every one was bypassable, for a reason read out of the installed companion rather than reasoned about: `resolveWorkspaceRoot()` walks **up** to `git rev-parse --show-toplevel`, so no placement of `--cwd` *inside* the durable root's repo ever narrowed anything. A by-path guard cannot close this, because the checked party shares the enforcement mechanism's access; only a categorically different capability does. Three changes carry it. (1) A per-invocation write-isolated sandbox: a fresh `mkdtemp()` `--cwd` proven outside any git working tree by running the companion's own `git rev-parse --show-toplevel` probe rather than pattern-matching the path, and failing CLOSED — a probe that times out or cannot run refuses to dispatch (`reason=sandbox-not-isolated`), since only a probe that actually ran can license one, with output leaving via an fd-pinned, digest-verified copy (`O_NOFOLLOW` source, fstat identity+digest, destination held as an `O_DIRECTORY` fd so nothing re-resolves by path). (2) `--durable-root` and `--plugin-root` as two orthogonal, optional inputs — data vs. executable resolution, the latter **never** derived from the former, since resolving a checker from inside the tree it checks would let a tampered copy pass itself. Exactly four scripts accept `--plugin-root`; `cache_key.py`, `draft_sha1.py` and `ledger_update.py` are leaves that do not, and a flag accepted but never read would make callers and tests read as hardened while nothing changed. Scope stated plainly: **seven** scripts spawn a sibling from the durable-root copy, and four of them are hardened here. Proven with poisoned-sibling fixtures asserting both that the flag bypasses the tampered copy *and* that omitting it genuinely runs it — without that second half the first is vacuous. Omitting both flags is byte-identical to the previous self-anchored behavior. (3) A new optional `engine.max_codex_jobs_per_batch` (default 400) refusing an over-large batch before the first dispatch. **Stated because it isn't flattering:** at shipped settings that gate is redundant at every row — `batch_agent_cap` binds first — and review made it *more* so, catching that it counted the Claude fix rounds as codex jobs; the true cost is `max_fix_rounds + 2` per segment (1 translate + `max_fix_rounds + 1` reviews), not `2*max_fix_rounds + 2`, so it had been refusing batches genuinely within the cap. **Residuals, stated rather than claimed closed:** process-group termination is *not* implemented — the detached worker's pid is never recorded, so `terminateProcessTree(pid ?? NaN)` no-ops; what replaces it is isolation, not proof of death (single-use sandbox, never read after timeout, unconditionally `rmtree`'d). And three gate paths still resolve executables from `${durable_root}/scripts/` — `codex_job.py`'s `_trusted_scripts_dir()`, `canon_validate.py` → `cache_key.py` (which stamps the hashes gating canon reuse), and `final_audit.py` → `select_segments.py`. **The sandbox does not close these:** it confines only the codex processes this driver launches, while the glossary and skeptic passes dispatch `codex:codex-rescue` to write under `${durable_root}/` and the sanctioned manual W5 drive runs the companion with `--write` and `cwd = durable_root`. Since the glossary pass runs before W5 in the same project, a gate script tampered with there is what runs later — a live residual, not defense-in-depth. Closing it is not one line: there is no `{{PLUGIN_ROOT}}` substitution token, and `draft_ready.py`/`validate_draft.py` must adopt `--durable-root` first. **Upgrade consequence:** seven of the fourteen `PLUGIN_BUNDLE_MEMBERS` change bytes, so `plugin_bundle_hash` moves and **every converged segment in every project is marked stale** on the next run (#409).
- **Out-of-band W5 dispatch driver (v1.18.0)** — `segment_dispatch_driver.py` can drive W5 mass-translate as a local process instead of from inside agent calls: it resolves the resume `RUN_ID`, dispatches translate and review jobs, reads what the gates promote, and writes the ledger, with no 600-second per-call ceiling to work around. **It is optional and `pipeline()` remains W5's default.** It deliberately cannot perform the fix step — applying review findings is a content-editing LLM turn — so on a not-clean review it returns `needs_fix` with the round label, the findings and a rendered fix prompt, and stops; a human or a session drives that loop, and nothing ships that consumes the handoff unattended. Adding the driver to `PLUGIN_BUNDLE_MEMBERS` moves `plugin_bundle_hash`, so **every already-converged segment in every existing project reclassifies as stale on the next run**; Step 0a now carries the instruction to run `backfill_ever_converged.py` (dry run by default) before the first W5 on any project this plugin has touched before (#409).
- **Five defects in that driver and its codex worker (v1.19.0)** — all shipped in 1.18.0, found while building a dispatch mode that was then abandoned on design grounds. The driver's own **documented launch command could not dispatch anything**: self-anchored, it resolved `resolve_codex_companion.py` under `${durable_root}/scripts/`, where Step 0a deliberately never copied it, and exited 2 before rendering a prompt. The stated reason for that exclusion was false — the script reads no `__file__` and searches `~/.claude*/plugins/cache/openai-codex/**` from anywhere — which is why it survived every review it passed through; Step 0a now copies it, and the three remaining exclusions each state their own real reason. Because that destination was previously documented as never written to, a project that hand-adapted its own copy around the exit-2 defect could have one there: Step 0a **halts** on any pre-existing non-managed entry at that path — divergent file, symlink, directory — rather than overwriting it or auto-renaming it aside, names the path, and hands the operator SKILL.md's own per-kind recovery instructions — per kind because they differ: renaming a *symlink* aside preserves the pointer and not the adapted bytes behind it, so its resolved target has to be copied out first, whereas a divergent regular file can simply be moved. A refusal was chosen over a preserving backup deliberately: Step 0a's copy pass is prose an orchestrating session executes by hand, not atomic code, and a rename-then-copy cannot be made symlink-safe or concurrency-safe from prose alone. The workflow **template was unresolvable under a deployed durable root** (the self-anchored branch named a `templates/` directory Step 0a never creates), and the path to it was selected with `Path.is_file()`, which **follows symlinks** and collapses every lookup failure into "absent" — for a path that is then read and executed during prompt rendering, so a stray or planted file beside the scripts could take over the gate and every prompt; selection is now `lstat`-based and refuses ambiguity. An **unreadable-but-present canonical draft was silently replaceable** at both `os.replace()` sites, because `os.replace()` needs write permission on the directory rather than the target. And `_is_regular()` guarded `open()` but not `fstat()`/`close()`, so it could raise past the callers relying on it to answer False — and it never actually **read**, so on a network/FUSE filesystem or damaged storage the metadata calls could both succeed while a real read returned EIO/ESTALE, passing the very check that exists to catch it; it now drains the descriptor to EOF, a first-byte read having been tried and found insufficient (a regular file can serve a good prefix and fail on a later page, and the promote would still destroy the unread bytes). That drain runs while the process holds the per-segment lease inside a deadline-bound job, so it is bounded two ways — a 64 MiB ceiling checked from `fstat()` before any byte is read and re-checked against the bytes actually read (`st_size` is a snapshot; a file can grow past it), and the caller's own remaining *phase* budget re-checked between reads and again after EOF, the phase budget rather than the whole job's ceiling so that a poll-window operation cannot eat the reserved finalize tail. **An over-ceiling file, or one whose read outlasts that budget, is refused**, which is a trade-off rather than a free win: a legitimately huge canonical becomes unpromotable rather than accepted unread, on the principle that an unpromotable draft is recoverable and destroyed bytes are not. **What those two bounds do not cover is stated rather than papered over:** both sit *between* reads, so a single `os.read()` that never returns — a hung FUSE or network mount — is not bounded at all, `O_NONBLOCK` being no help since a regular file ignores it. A per-read timer was built for exactly that and removed before release: it would have been the script's only process-global signal handler, an inherited signal mask could silently defeat it (a defeat that looks identical to a real pass), and it would have guarded one of this file's three byte-copy loops while the other two, both older than this release, stayed unbounded. The drain shares their residual instead of claiming a bound it could not keep. When that guard refuses, the run stops and the validated attempt is left on disk rather than destroyed — but it is **not** recoverable by a later run, because its name embeds a per-invocation random component nothing reconstructs, and it is not inert either: it still matches the `*.draft.json` glob both dispatch-scan consumers use, so it can perturb draft counts and run evidence until removed. **The pending-slot lifecycle is otherwise untouched by this release, deliberately.** A fix for the deferral's unconditional overwrite — which destroys a previously validated candidate that has merely gone unreadable between runs — was built here and withdrawn: it is not one of the defects this release names, it predates the release it would fix, and three consecutive review rounds each found the flaw in the previous round's fix. The reason it is hard is now filed rather than carried in anyone's head: the two sites that write the slot have **opposite validation asymmetries** — at the deferral the fresh candidate is unvalidated by construction, at the canonical-refusal site it has passed every gate while the occupant probably has not — so no uniform rule is correct at both. **Stated rather than claimed closed, at the width the code actually supports.** Both canonical guards are check-then-`os.replace()`, and that gap is wider than "an unreadable file slips through": the guard observes the entry at one moment and the rename resolves the pathname again at another, so anything substituted at that path in between — including a perfectly readable *newer* canonical published by another writer — is destroyed without ever being observed. This repository's own suite pins that behaviour rather than forbidding it; the per-segment lock serialises cooperating `codex_job.py` processes and nothing else. And the template's **bytes are not authenticated**: the path is now resolved and read through a single no-follow descriptor, closing leaf and ancestor symlink substitution and the swap between checking and reading, but an ordinary regular file at the expected path still passes every check and is executed. The previous release executed that same path with a weaker check, so this is **structurally narrowed, not closed**. Third in the same family: clearing a non-regular squatter off a deterministic slot classifies the entry with `lstat()` and then removes it by pathname — two syscalls, not one atomic operation — so a writer substituting a real file in that window has it destroyed, and if the substitute is a *directory* the destroy is recursive. That helper is unchanged from the previous release, call sites included. **One line governs which defects above were fixed and which are listed here:** a data-loss path reachable without a hostile concurrent writer is fixed when the fix is small; a race that requires a writer ignoring the per-segment lock, and new machinery to close, is disclosed. **Two of the three limits are that second kind; the template one is not** — an ordinary regular file at the expected path, placed at any earlier time by anything, needs no race and no timing at all. It is disclosed for a different reason: authenticating content requires a trusted digest or signed value, a capability this release does not have rather than a lock it declines to take. Grouping it with the races understated it, and the grouping is corrected here rather than quietly dropped.
- **A non-clean mandatory final review could never converge, even after every finding was applied (v1.20.0)** — `derive_next_action()` returned `cap_reached` unconditionally the instant the stored review at round label `"final"` was non-clean, reading only the cached review verdict and never checking the same draft-sha1 comparison the clean branch right above it already computes for its own use. The neighbouring clean branch already guards the identical situation — a clean review whose recorded `draft_sha1` no longer matches the current draft is re-reviewed rather than trusted, because `ledger_update.py` refuses a convergence write whose `draft_sha1` disagrees with the current draft — but the non-clean final branch had no equivalent guard, so re-selecting a segment that landed there kept reporting it capped, even once its findings had been applied by hand and `validate_draft.py`'s deterministic structural/coverage check — not a literary judgment — came back clean; a capped segment is not re-selected by default, only by explicitly naming it again under the same run. It now reuses the same draft-sha1 discriminator: a demonstrably current final review still caps, but a final review the draft has since moved past gets a fresh review at the same `"final"` label instead. Editing the driver moves `plugin_bundle_hash` again, so every already-converged mass segment reclassifies as `stale` at the next Step 0a refresh — the `.ever_converged` sentinel gate from 1.18.0 refuses to dispatch a segment whose sentinel actually exists without `--allow-retranslate-converged` rather than silently retranslating it, but a project that converged segments before that gate existed (before 1.18.0) and was never backfilled has no sentinels to refuse with: see SKILL.md's own upgrade note and run `backfill_ever_converged.py` before the next W5 dispatch on such a project. That sentinel also covers only a segment that converged at least once — one that never did has none — so a fresh `RUN_ID` (which the same bundle-hash move mints on the next invocation) makes `draft_ready.py`'s token check reject its existing draft and fall through to a full re-translate, discarding any fix already hand-applied to its draft. That is the outcome for a segment capped BEFORE the upgrade, because the bundle-hash move mints the new `RUN_ID` underneath it — not a general rule about capped segments: one capped under the current bundle can be named again under the same `RUN_ID`, is durably reopened, and gets a fresh review at the `"final"` label, which is the whole point of the driver fix above. An earlier draft of this note called re-translation "the only path", which is true only for the pre-upgrade case it was describing.
- **Three ambiguity-resolves-toward-the-irreversible defects in that fix, and one sentinel predicate (v1.20.0)** — a codex review of the fix above found the driver minting a terminal `cap_reached` verdict whenever a draft sha1 could not be computed: an uncomputable hash now fails the invocation with no ledger write and no codex job rather than capping, and a missing reviewed hash reopens at the `"final"` label, because capping is the one outcome a human has to undo by hand. The reopen is also confirmed on disk before the review is dispatched, so a crash mid-window cannot leave a segment dispatched against a cap that was never recorded. Separately, the `.ever_converged` marker that decides whether a converged segment may be re-dispatched had its existence decided four different ways — three `.exists()` readers plus a writer inferring it from `O_CREAT|O_EXCL` raising `FileExistsError` — which could then disagree with each other about the same file — and a writer that believes a segment protected while the dispatch gate believes it unprotected is precisely how converged work gets retranslated. One byte-identical three-state predicate now replaces all four of those decisions: a marker that exists but cannot be classified is neither present nor absent, which `.exists()` cannot express — and it did not fail in one direction, folding a dangling symlink toward absent and a directory at the marker's path toward present. The three states map per caller on purpose — the writer and the dispatch gate refuse, the audit counts (an audit must never call a converged book undeliverable because of a stat failure), and the backfill reports it unprotected. Sharing the predicate turned out not to be sufficient on its own: inside the audit, the carve-out count and the ambiguity diagnostic still asked the same path two separate questions with a `stat` each, so a sentinel that changed between the two reads was counted as carved out and reported by nothing — the exact silence the count's own comment promised was impossible. Both now consume one scan, and a missing entry raises rather than silently re-reading. Not fixed here, and stated rather than implied: the codex-job budget overspend is unbounded across invocations, not the "one extra job" the review reported; the cap write is still check-then-write, so a draft replaced in that window gets a cap over unreviewed bytes; and A→B→A is undetected in two windows, not one. See the Known limitations in [`plugins/literary-translator/CHANGELOG.md`](plugins/literary-translator/CHANGELOG.md) for all three and the operational rule they imply — do not hand-edit a draft from the moment a dispatch for it is decided until its review lands.
- **A third admission profile for a stalled, previously-converged unit — proved facts kept separate from a disclosed operator assertion (v1.24.0)** — `--from-stalled` closes the last population #438's re-review machinery couldn't reach: a segment previously converged, then left with genuinely incomplete bookkeeping (materialized `in_progress`, the `.ever_converged` sentinel present, no `reviewed_draft_sha1`, a stored review stale against the current draft) — until now handled entirely by a hand-driven `ledger_update.py` procedure this project's own operator rule prohibits running by hand. The profile proves exactly two liveness facts from the durable root's own kernel state — no competing driver holds `runs/.driver.lock`, no codex job holds this segment's own `.codex_job.<seg>.lock` — and states the remainder (no Workflow fix turn, no other claim invocation touching the same ids) as an explicit, disclosed operator assertion the plugin cannot verify, spelled out identically in the flag's `--help`, its refusal text, and SKILL.md. An admitted id still can never be translated, still clears `previously_converged` for itself only, and the pre-existing hand procedure survives as the fallback for a unit that fails the profile.
- **The no-exit stale state: a converged unit with an untouched draft finally has a route back (v1.25.0)** — a converged segment whose draft was never hand-edited but whose cache key moved could reach neither `--from-converged` (which required the draft to differ), nor `--from-stalled` (which requires no `reviewed_draft_sha1`), nor `--from-cap` (which requires no `.ever_converged` sentinel), nor the plain path (which dies on the `previously_converged` fatal) — and `assemble.py` requires every manifest segment `converged`, so such a book could be neither re-reviewed nor assembled (#491, #490). Two changes close it, without adding a fourth admission profile. `assemble.py` now carves out a `stale` record whose staleness is machinery-only (`plugin_bundle_hash`, `schema_hash`, `derivation_bundle_hash`), a non-ABSENT `.ever_converged` sentinel, and a draft still matching `reviewed_draft_sha1` — the same population `final_audit.py` already reports `project_complete: true` for, so the two gates now agree on one materialized snapshot instead of one refusing what the other already certified. `validate_assembled.py`, the hard structural-completeness gate that runs before Deliver, is widened to the same population — carving out assembly alone would have moved the wall one gate earlier rather than removing it. And `--from-converged` widens its own drift baseline: a unit whose draft is untouched but whose cache key moved on a content-affecting field (not machinery-only) is admitted for re-review, same as a hand-edited unit always was; the re-review then re-keys it. Assembly gains no new write and no new subprocess, and admission stays permissive-only — nothing admitted before this release is refused after it. Deliberately out of scope: assembly still trusts the same snapshot it always has, so a content input edited after the last `ledger_merge.py` run stays invisible to it, identically for `converged` and for carved-out `stale` records — filed as a separate product-wide issue rather than solved here.
- **A refused claim dispatch no longer mutates the tree (v1.38.0)** — `select_segments.py`'s claim block published its two durable writes — the claim record at `runs/<RUN_ID>/.claimed.<seg>`, and a rewrite of the segment draft's own `dispatch_token` to that run — *before* three refusals that can still fire (`previously_converged`, an unsafe prior `RUN_ID`, a prior run with no resume-integrity digest). A dispatch those gates declined therefore left a claim naming a run that did no work, and because `ledger_update.py` requires a draft's token to match the run recording its convergence, the next round would translate, review and come back clean and *then* fail to record the result — a structured failure after the work was already paid for (#538). Both writes now run after every policy refusal, immediately before the selector returns, so there is no code path from a durable write back to one of those gates; admission is unchanged, and a claim that is admitted still publishes exactly as before. A refusal caused by a claim WRITE itself failing is a different case and can still leave a partial one; that refusal names the ids it managed to write. `SKILL.md` also now states what `resume: true` does and does not assert — digest identity, never evidence that the resumed run dispatched anything — since a refused claim run leaves its own run directory behind for a later invocation to match legitimately (#544).

### Status & scope

- Source-language extraction is proven against **Historiettes' 17th-century French specifically**, not French in general; every other language config — and every other French source — ships as an unverified **starter preset gated by a mandatory smoke test** (see `plugins/literary-translator/skills/literary-translator/references/language-pair-parameterization.md`).
- The shipped `gutenberg_epub` source adapter and the expert-mode `custom` extractor remain **experimental / unstable** until each is pilot-proven end-to-end on a real project (see `references/source-format-adapters/`); `plain_text` is specified but not yet implemented (#62).
- Scoped to texts whose natural segments / chapters fit under a configurable per-segment word cap; a novel with genuinely long natural chapters is out of scope for v1.
- The v1.2 Workflow-orchestration reliability pass is new plugin hardening, not itself pilot-proven — a real end-to-end pilot run is still the honest gate before treating it as fully load-bearing; a synchronous codex block-and-hang on a DISPATCH call's own `await` remains an accepted residual risk (see `references/gotchas.md`).

## `multi-profile-plugins` — v1.0.0

Understand and diagnose how Claude Code stores plugins when several `CLAUDE_CONFIG_DIR` profiles run on one machine. If some profiles share a `plugins/` directory (commonly via symlinks), a `claude plugin` op from one profile re-stamps the shared `known_marketplaces.json`, and every other profile then fails with `Marketplace 'X' has a corrupted installLocation …`; catalog-scoped garbage collection can also delete a plugin version one profile still uses. This plugin explains the mechanism and ships a read-only health check — it does **not** perform an automated migration.

Trigger phrases: "corrupted installLocation", "claude plugin across profiles", "CLAUDE_CONFIG_DIR profiles", "plugin deleted in one profile", "multi-profile plugins" — full list in `plugins/multi-profile-plugins/skills/multi-profile-plugins/SKILL.md`.

### What it covers

- **The skill** — when to keep profiles' plugin stores shared vs. independent, why a shared store causes "corrupted installLocation" churn and cross-profile deletion, and (conceptually) the structural fix: give each profile its own independent `plugins/` store. Diagnosis-and-reasoning only; no bundled destructive tool.
- **The deep mechanism** (`references/cli-mechanism.md`) — the reverse-engineered `installLocation` prefix check (`path.resolve` + `startsWith`, no symlink resolution; cache-managed sources checked while `file`/`directory`/seed skip it), the catalog-scoped startup GC + `uninstall` that force full content independence rather than just a de-shared registry, the per-config-dir cache model, and the "a read-only probe can mutate state" caveat.
- **The health check** (`scripts/inspect_profiles.py`) — a read-only, stdlib-only Python script that auto-detects `~/.claude*` profiles (or takes explicit ones), reports which `plugins/` dirs are real vs. symlinked, flags profiles that share a `known_marketplaces.json` (the churn risk) **or a content store** (`cache/`/`data/`/`marketplaces/`/`.install-manifests/` — the cross-profile deletion vector a de-shared registry alone doesn't fix), and detects cross-profile pointer leaks via exact path-prefix matching (never a substring grep). Exits non-zero on any warning.

### Scope

Knowledge + read-only diagnostics. Converting a shared store to independent per-profile stores touches live plugin data and is intentionally left as a deliberate, backed-up manual step — not an automated action this plugin performs.

## License & disclaimer

MIT — see [`LICENSE`](./LICENSE). No warranty, no vendor affiliation — see [`DISCLAIMER.md`](./DISCLAIMER.md).
