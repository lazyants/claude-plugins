{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "final-audit-summary.schema.json",
  "title": "Final Audit Summary",
  "description": "Structured stdout of final_audit.py, run at W7 over every converged segment: 2 hard checks (coverage, stale-review) rolled into hard_failures, 4 WARN checks rolled into warnings, a whole-project completeness gate (completeness_counts/project_complete/stale_previously_converged), and a frontback coverage report. The arithmetic rollup invariant hard_failures == coverage_failures + stale_review_failures is enforced procedurally by final_audit.py itself. project_complete is true if and only if every one of completeness_counts' five values (not_started, recoverable, stale, blocked_needs_regeneration, human_escalation) is 0, EXCEPT for the #409 Step 2 carve-out: a 'stale' segment that also carries the durable .ever_converged.<seg> sentinel (an already-finalized translation whose cache key merely moved) does not count against project_complete -- see stale_previously_converged. This forward direction (all five categories genuinely 0 implies project_complete true) is enforced by this schema below; the carve-out-aware direction (completeness_counts.stale - stale_previously_converged == 0 implies project_complete true) is a cross-field equality this schema cannot express and is enforced procedurally by final_audit.py's own compute_project_complete().",
  "type": "object",
  "properties": {
    "coverage_failures": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of converged segments that fail the coverage hard check."
    },
    "stale_review_failures": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of converged segments that fail the stale-review hard check."
    },
    "hard_failures": {
      "type": "integer",
      "minimum": 0,
      "description": "Rollup invariant (procedurally enforced, not schema-expressible): hard_failures == coverage_failures + stale_review_failures."
    },
    "warnings": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of WARN-level (non-blocking) check failures across the 4 WARN checks."
    },
    "project_complete": {
      "type": "boolean",
      "description": "Rollup invariant (procedurally enforced by final_audit.py's compute_project_complete(), see this schema's own top-level description): project_complete == true if and only if every one of completeness_counts' four non-'stale' values equals 0 AND completeness_counts.stale - stale_previously_converged equals 0."
    },
    "completeness_counts": {
      "type": "object",
      "description": "Whole-project completeness gate: classification counts from a full select_segments.py run over the entire manifest.json with no --only-segs restriction. 'stale' here is the RAW count -- never reduced by the #409 Step 2 carve-out (see stale_previously_converged) -- so an operator can always see the true total.",
      "properties": {
        "not_started": { "type": "integer", "minimum": 0 },
        "recoverable": { "type": "integer", "minimum": 0 },
        "stale": { "type": "integer", "minimum": 0 },
        "blocked_needs_regeneration": { "type": "integer", "minimum": 0 },
        "human_escalation": { "type": "integer", "minimum": 0 }
      },
      "required": [
        "not_started",
        "recoverable",
        "stale",
        "blocked_needs_regeneration",
        "human_escalation"
      ],
      "additionalProperties": false
    },
    "stale_previously_converged": {
      "type": "integer",
      "minimum": 0,
      "description": "#409 Step 2: count of completeness_counts.stale segments carved out of the project_complete gate because they (a) carry the durable .ever_converged.<seg> sentinel AND (b) went stale ONLY because a field in the machinery-only allowlist (plugin_bundle_hash, schema_hash, derivation_bundle_hash -- tooling/schema/derivation-code fingerprints that can never by themselves change translated prose) moved, never a content-affecting field (e.g. style_contract_hash, prompt_hash, used_terms_hash, the source text itself) nor a draft hand-edit since review (stale_reason draft_sha1_mismatch). A 'stale' segment failing any of these is never counted here and still blocks project_complete exactly as before (fail-safe direction)."
    },
    "frontback_coverage": {
      "type": "array",
      "description": "UNCONDITIONALLY present as an array -- one entry per manifest frontback[] item. An empty array is used for a plain_text source with no frontmatter; this field is never omitted.",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "decision": {
            "type": "string",
            "enum": ["translate", "regenerate", "omit"]
          },
          "status": {
            "type": ["string", "null"],
            "description": "The matching segment's classification for a translate decision; null for regenerate/omit decisions."
          }
        },
        "required": ["id", "decision", "status"],
        "additionalProperties": false,
        "allOf": [
          {
            "if": {
              "properties": { "decision": { "const": "translate" } },
              "required": ["decision"]
            },
            "then": {
              "properties": { "status": { "type": "string" } }
            }
          },
          {
            "if": {
              "properties": { "decision": { "enum": ["regenerate", "omit"] } },
              "required": ["decision"]
            },
            "then": {
              "properties": { "status": { "const": null } }
            }
          }
        ]
      }
    },
    "generated_at": {
      "type": "string",
      "description": "Timestamp at which final_audit.py produced this summary."
    }
  },
  "required": [
    "coverage_failures",
    "stale_review_failures",
    "hard_failures",
    "warnings",
    "project_complete",
    "completeness_counts",
    "stale_previously_converged",
    "frontback_coverage",
    "generated_at"
  ],
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "completeness_counts": {
            "properties": {
              "not_started": { "const": 0 },
              "recoverable": { "const": 0 },
              "stale": { "const": 0 },
              "blocked_needs_regeneration": { "const": 0 },
              "human_escalation": { "const": 0 }
            },
            "required": [
              "not_started",
              "recoverable",
              "stale",
              "blocked_needs_regeneration",
              "human_escalation"
            ]
          }
        },
        "required": ["completeness_counts"]
      },
      "then": {
        "properties": {
          "project_complete": { "const": true }
        },
        "required": ["project_complete"]
      }
    },
    {
      "//": "The reverse direction (project_complete true) does NOT imply completeness_counts.stale is 0 -- the #409 Step 2 carve-out lets project_complete be true with a nonzero raw 'stale' count, provided every one of those segments carries the .ever_converged sentinel (completeness_counts.stale - stale_previously_converged == 0, a cross-field equality this schema cannot express -- see this schema's own top-level description). So this block only constrains the four categories the carve-out never touches.",
      "if": {
        "properties": {
          "project_complete": { "const": true }
        },
        "required": ["project_complete"]
      },
      "then": {
        "properties": {
          "completeness_counts": {
            "properties": {
              "not_started": { "const": 0 },
              "recoverable": { "const": 0 },
              "blocked_needs_regeneration": { "const": 0 },
              "human_escalation": { "const": 0 }
            },
            "required": [
              "not_started",
              "recoverable",
              "blocked_needs_regeneration",
              "human_escalation"
            ]
          }
        },
        "required": ["completeness_counts"]
      }
    }
  ]
}
